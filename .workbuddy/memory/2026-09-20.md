# 2026-09-20

## 补完第二轮拷问里"我承认但没改"的五处

上一轮（09-18）在 `docs/interview-qa-round2.md` 里主动列了 6 处对我不利的事实，
但只承认没改。今天把它们从"我承认"改成"可测的"。

**新增前提敏感度实验**（`scripts/sensitivity_premise.py` → `examples/premise-sensitivity.txt`）：
核心判据「没标 `default` 即必填」是我自己设的口径（该数据集 `required` 字段为 0），
整个翻转再跑一遍，回答"押错了会怎样"：

| 处置 | P1 我的口径 | P2 严格口径 |
| --- | --- | --- |
| ✓ 正样本 | 58649 | 58649 |
| 🔧 待修 schema | 1286 | **0** |
| ⚠️ 需人工 | 65 | **1351** |
| ✗ 丢弃 | **0** | **0** |

**丢弃数不因前提翻转而变** → "这批样本不会被误杀"不依赖我押对了哪个前提，
所以不需要去赢那场优先级之争。P2 的产出是 1351 条人工队列（按队列长度判据
等于没有队列），这是 P1 在**操作上**更好的理由——不需要主张它在**认识论上**正确。

**踩到的坑（值得记）**：实验第一版 patch 的是 `_description_claims_default`，
而判定路径 `_issues_against` 实际走 `default_claim()` + `misplaced_defaults()`
——**patch 没生效，两遍跑出一模一样的数字，"结论稳健"差点是个假结论**。
修法：加断言，先用一个必然命中的样本证明 patch 生效，再看数字。
**通用教训：对照实验必须先证明"两臂真的不一样"，否则 0 差值分不清是稳健
还是没生效。**

**另修两处我自己犯的措辞错**（都是我在抓别人的同类错）：
1. 报告第二节原标题「根因分布」，下面躺着 `ok`（=未命中）→ 改为「判定分布」+ 注。
   把未命中写进叫「根因」的表里，等于我替这批数据判过了。
2. 报告第八节把「数据源没声明必填」写成「**因此**模型省略是合理的」——
   前半是事实，后半是我加进去的推断，拆开写。

**CHANGELOG** 追加 v3.0.2（值错位证据 / 假探针自删 / 前提敏感度 / 全量口径 / 措辞修正）。
提交 `171334c`。364 例测试全绿，ruff 全绿。

## 项目当前阶段（给用户做的 STAR 汇报时核过的）

- 起点 2026-08-21，到 2026-09-18 共 **60 个提交 / 约 4 周**；tag：v0.5.0 / v0.6.0 / v2.2.0
- 四段：M1–M10 数据质量引擎（v2.0.0）→ R2 增强模块（v2.1.0）→
  R3 通用包（v2.2.0）→ **v3.0 主线切到 Agent 训练数据质检（当前）**
- 当前状态：**开发收尾 / 材料就绪，进入投递期**。主线闭环已通（一条命令
  全量 6 万条 / 16 秒），不是"进行中的功能"，而是"能交付 + 知道哪有洞"

---

## R5.0 落地：v4.0 的实验台（提交 dc5a022 → a16da4c）

v4.0 = 「准入决策」第三次换域（v1 数字能不能引用 → v3.0 轨迹能不能喂训练 →
**v4.0 这段内容该不该进上下文**）。R5.0 只做前置实验台，不做结论。

**新增**：`src/findata/contextbudget/`（6 文件）、`scripts/probe_ollama_tools.py` /
`run_context_audit.py` / `compare_audit_arms.py` / `measure_tools_token_cost.py`、
`tests/test_contextbudget.py`(19) + `tests/test_audit_analysis.py`(13)、
`docs/r5.0-acceptance.md`。测试数 364 → **397**（四处文档同步）。

**关键设计**：不用 langchain-openai / langgraph（框架会往 payload 里塞自己的东西，
混进 `prompt_tokens` 后**无法剥离** → 账单不可归因）。手写薄循环换全程可归因。

### 本轮最有价值的三个发现

1. **实测替换算**：工具定义的 token 成本**不要用字符数除系数估**——
   同一条 payload 发两次、只切换有没有 `tools`，读上游 `usage.prompt_tokens` 求差。
   实测（7B）：无工具 45 / 4 工具 613 / 27 工具 **2277**；4→27 多花 **1664 token/次**
   （边际 72.3/工具）。**自检**：同 payload 连发 3 次，prompt_tokens 逐次完全相等。
   同时**主动拆掉自己的虚高陷阱**：「占 92.7%/98%」是短 prompt 的产物（探测器其余
   部分只有 45 token），**绝对增量才可迁移**，占比必须同时给分母。
2. **`model mismatch` 的代价是双向的**：同 harness / 同任务 / 同工具清单，
   3B 命中 **0/6**、7B **4/6**，而 3B **prompt_tokens 更高**（30,548 > 28,516，
   多跑一轮多调一次工具）。**换小模型既贵又不准。** 失败原因是具体行为：
   把 `prefix` 传成 `{'path: ': ...}`（**值对、键名错**），独立复现 TSCG 结论。
   处置顺序很重要：**先把自己的错误信息改成可行动的，再去看模型能不能自修**
   ——反过来就是拿模型的锅盖自己的 bug。
3. **`reset_for_test()` 声明与实现不符**（既有缺陷，本轮撞出来）：OTel 的
   TracerProvider 是**进程级单例**，`set_tracer_provider` 第二次被忽略 →
   清缓存后新建的 provider 不生效，span 仍导进**旧** exporter，而
   `get_in_memory_exporter()` 返回**新** exporter → 测试**看起来在验证，其实
   什么都没验证**（当时表现是一个 IndexError，真凶是不一致）。
   **通用教训：「静默不验证」只能靠身份断言抓，数值断言抓不到。**

### 本机环境坑（新增，值得跨会话复用）

- **系统代理拦 localhost**：`curl http://localhost:11434/api/tags` 返回 **502**
  是**代理**报的错，不是 Ollama 报的错。httpx 必须 **`trust_env=False`**。
  Bash 里探活加 `--noproxy '*'`（这样拿到的 000 才是"服务真没起"）。
- **`uv run python` 看到的 `/tmp` 不是 Git Bash 的 `/tmp`**：Bash 里 `> /tmp/x.txt`
  落在 `C:\Users\10393\AppData\Local\Temp\x.txt`，但 Python 里 `Path('/tmp/x')`
  会去找 `C:\tmp\x`。**从 Bash 传路径给 Python 要用 Windows 原生绝对路径。**
- **Bash 里连 `grep` / `head` / `tail` / `sed` 都没有**（WorkBuddy shim 覆盖 PATH）：
  统计行数用 `uv run python -c` 代替，读文件用 Read 工具。
- **`ruff format --check` 不是门禁**：本仓库 75 个既有文件都会被重排，CI 只跑
  **`uv run ruff check .`**（`pyproject.toml` 无 `[tool.ruff.format]`）。别顺手格式化。

### 流程教训

- **提交自洽性**：我第一版把 `tests/test_contextbudget.py` 和它的依赖包拆进了
  两个提交 → 那个中间提交 checkout 出来跑不过（测试 import 不到包）。
  用 `git reset --soft HEAD~1` 重做成一个自洽提交。
  **判据：任何单次 checkout 出来，测试套件都必须能跑。**
- **中文提交信息用 `git commit -F .git/COMMIT_MSG_TMP.txt`**，别用 `-m "..."`
  （消息里的引号会打断 shell 引用）。
- 文档一致性门禁会因**加测试**而红（要等于 `pytest --collect-only` 真实数），
  改完测试记得同步 README / PITCH / docs/handoff.md。

### R5.1 的前置条件（**不改数据模型就测不了**）

R5.1 的核心指标是「**被后续引用的字段集 vs 从未被引用的字段集**」，但当前
runner **只记 `payload_chars`、不记返回内容**，且工具返回是散文式文本——
**没有"字段"这个粒度**。所以 R5.1 的第一件事是改数据模型（结构化返回 + 字段级
字符数 + **可判定"是否被引用"的最小信息**：值指纹而非全文，PII 与体积都不允许）。
**不写下来，下一阶段就会拿 payload_chars 硬编一个"字段利用率"，那是指标造假。**

---

## Jev 爆火事件（用户提问触发）：用得上，而且有一条是「别自己造」

**Jev** = TypeSafe AI 2026-09-15 发布的 **System One 决策模型**（闭源、云端 API、无权重）。
不生成文本，只答三种**带类型**的问题：`Choice`（≤255 选项）/ `Score`（有序打分）/
`Noul`（Bernoulli 真假概率），并行评估返回**校准过的概率**。定位「ChatGPT 给人写答案，
**Jev 给程序做判断**」；命名取自**杰文斯悖论**。

**必须打折的（第三方审计 `SamuelSacco/jev-exploration`）**：官方称快 20–400×，
独立实测 **5–25×**；海外延迟实测 **1.6–3.7s**（不是 70–500ms）；准确率**与中档模型持平**；
「不会幻觉」**不成立**（官方文档自己写：类型化输出保证**接口**不保证**真相**）；
**概率校准无公开曲线 = 没法验证**——而校准恰恰是能用它做分流的前提。

### 竞争信号（这条最重要）：`fast-jev-compaction` 离我的命题很近

`tamaratran/fast-jev-compaction`（约 3 天前建仓）自称「用 Jev 决策**替换 Claude Code 的
compaction 摘要**：每个工具调用与结果被打分，过期的丢弃或截断，保留的逐字节不变」。

我读了它的 How it works。**关键一条：送给 Jev 的 state 里，每个工具结果被替换成一行
提示（`ok, 4213 chars (omitted)`）—— 即 Jev 看不到工具返回的内容。**
它只按 goal + 调用输入 + 对话文本问两个 `Noul`：「这个调用该留吗」「这个结果该逐字留吗」，
按 `keepThreshold=0.5` 分三档：都留 / **留调用+结果截断到 300 字符** / 都删。

→ **它仍然是"预置型判断"，只是把规则换成了极快模型；判断依据是合理性，不是证据。**
我的缝隙（证据来自 **trace 里真实发生的引用**）**还在**。§9.5 那条判断因此**成立**，
但要收窄一句话：**「证据不足→可逆处置」不是我的差异化，是共识做法**（它自己也这么做）；
我只剩两样，且都必须靠实测站住：**①证据来自 trace；②有配对验证闭环**。
**风险**：它有分发渠道 + 决策模型，真有价值的话他们两周内能补上这一层——
所以**稀缺的不是"裁剪的点子"，是"能证明裁剪没伤结果的验证闭环"**。

### 落地决定

1. **不自己实现「模型路由」那一臂**——Jev 已是事实标准（LangChain `TypeSafeClassifier`、
   Vercel AI SDK 7 `evaluate`、`eve` 的 auto model selection 默认评估器）。
   R5.2 原设计「字段裁剪 vs 模型路由」**作废**（两者不同层，比不出东西）。
2. **换成同层对手**：「合理性判断裁剪」（Jev 式，结果省略后再问）vs
   「trace 反推裁剪」（我的）——同任务、同质量尺、配对重放。
   若用模型复刻它的协议，只能写「**协议仿真，不是 Jev 本体**」。
3. **不用它做我的第二层语义判据**（`attribution.py` 明确拒绝语义匹配的那一层），
   除非先在自标注集上量出**校准曲线**并写进产物。
4. **不把付费闭源 API 接进 CI**（要 key、无权重 → 门禁里埋外部依赖）。
5. **不因它爆火改主线**：它降低的是**判断单价**；按杰文斯悖论，判断单价降 →
   判断总调用量涨 → 对"内容该不该进上下文"这类高频准入决策是**顺风**。

已写入 `docs/competitive-landscape.md` §10 + `docs/ROADMAP.md` R5.2（改设计）。

---

## R5.0 全量两臂跑完：**这是目前最强的单个结果**

32 任务 × 2 臂，唯一变量 = 工具清单长度（27 vs 4），模型 `qwen2.5:latest`。

| | `all7b`（27 工具） | `active7b`（4 工具） |
| --- | --- | --- |
| prompt_tokens 合计 | **223,423** | **104,079** |
| 平均每轮 prompt | 2,659.8 | 1,224.5 |
| 关键词命中 | 7/32 | **13/32** |
| 非 ok 结果码 | 28/54 (51.9%) | 22/55 (40.0%) |
| 沉默工具被调用 | **0**（从头到尾） | — |

**三组数字（R5.2 要的那三组，在"定义侧"这条杠杆上已齐）**：
1. **token 降 53.4%**。配对差分 正差 30 / 负差 2 / 相等 0，均值 +3,729.5，
   **符号检验 p = 2.46e-07**。
2. **成本降 ≈ 53.4%**（同模型同单价 ⇒ 比例=token 比例）；按示例单价 $2.50/M
   折算省 $0.2984 / 32 任务（**只算 input，单价须核实**）。
3. **成功率不降反升**：7/32 → 13/32，**McNemar p = 0.03125**，
   6 个不一致对**全部**朝短清单方向（仅 A 命中 = 0）。

**敏感性检验（照 v3.0 前提敏感度实验的做法）**：两臂各 1 次超时，且**不在同一个
任务上**（A 侧 t08 / B 侧 t27）→ 三个口径都算了一遍：
全 32 对 p=2.46e-07 / 降 53.4%；剔 t08 → 53.8%；**剔两侧 → 52.0%、p=8.68e-07**。
**结论不依赖那两个超时。** 反例留档：**2/32** 个任务上短清单更贵（都是关于本项目
自己新代码的任务）→ **"短清单一定更省"是错的**，只有配对分布上成立。

**交叉验证**：§2.1 用受控探针直接实测的工具定义 token 差 = **1,664/次**；
端到端两臂平均每轮 prompt 差 = **1,435.3/次**（1,435/1,664 ≈ 86%，残差来自
轮数构成/返回长度/命中数不同）。**两条独立测量路径互相印证**——工具定义确实在吃
prompt。但**不能写成恒等式**：端到端差分本来就不该等于受控探针差值。

**工具定义占 prompt 的比例：83.9%（27 工具）/ 46.4%（4 工具）——分母是本批短任务
的平均 prompt（2.7k / 1.2k token）**。换成长对话会大幅下降；可迁移的是**绝对量**
（27 个定义每轮固定吃 2,232 token，其中 23 个一次没被调用）。

**新发现（与预期不同）：一半工具调用没落好**（非 ok 结果码 51.9%，`get_signature`
65% 是 not_found = 模型在猜不存在的路径）。**但它不能叫"失败率"**——`not_found`
对搜索类调用可能是正常结果。它是**独立于"定义太长"的另一个浪费来源**，两者不许
合并成一个数字。

**边界**：这条杠杆是**定义侧**，比 R5.2 原定目标（**返回内容**裁剪）容易得多。
它证明的是**方法闭环可行**（测量→裁剪→同批任务重放→三组数字），
**不是**"最难的裁剪问题已解决"。23 个沉默工具是构造的 → **幅度不能当行业事实**。

---

## 【本日最重要】自查发现：我差点用一份**移动的语料**下结论

**发现方式不是自律，是一个交叉核验脚本。** 判据：`payload_chars` 是确定性量
⇒ 同一个 `(任务, 工具, 参数)` 在两臂字符数不同，只能是工具读到的磁盘内容变了。

**证据（4 处 / 570 字符）**：

| 调用 | 臂 A | 臂 B |
| --- | --- | --- |
| `list_files docs/` | 287 | 311 |
| `list_files scripts/` | 801 | 865 |
| `list_files tests/` | 833 | 916 |
| `read_file docs/ROADMAP.md` | 21,662 | 22,061 |

**用 `git ls-tree` 反推回提交，成因完全确定**：臂 A 的三次 `list_files` 读到了
**三个不同的目录状态**（早于 `a16da4c` / `dc5a022` / 早于 `a16da4c`）——
因为那一臂从 18:35 跑到 18:57，而我在 18:35–18:48 **往仓库提交了 4 个提交**。
⇒ **同一臂内部就不自洽**，而产物里没有任何东西能证明这件事发生过。

**规模与方向（两个都要写）**：
- 570 字符；按最不利的 1 字符 = 1 token（上界）也仅占 token 差 119,344 的 **≤0.48%**；
- 四处漂移**全部**是 A 更小 ⇒ 语料相同时 A 只会更贵 ⇒ **漂移是保守的**，
  结论不是被它撑起来的（方向对我有利这件事必须说，因为"没威胁结论"不等于"可以不说"）；
- **未核的部分**：只覆盖两臂共有的 20 个调用（**20/107 = 18.7%**），
  各自独有的 67 个**无法核对**。正确表述是「已核的有 4 处，未核的不知道」。

**根因不是这 570 个字符，是一句话没有产物能证伪它**：
`compare_audit_arms.py` 把「唯一变量是工具清单长度」**硬编码**在"诚实说明"里打印。
⇒ **一句打印出来的声明，如果没有任何产物能证伪它，它就不是诚实说明，是口号。**

**修补（五件，见 `docs/r5.0-acceptance.md` §3.5）**：
① `corpus.py` 语料指纹（边界唯一来源 + 内容 sha256）；② 账单加 `reproducibility`
段（提交/脏标记/语料指纹/工具清单指纹）；③ 对照脚本**核对指纹**而不是声明，
另加 `--require-same-corpus`（不通过就非 0 退出）；④ `check_corpus_drift.py`
= **第二条独立路径**（回看落盘 trace，能发现"跑的时候变了"，指纹发现不了）；
⑤ `--corpus-root` 指向**冻结快照**。

**两条独立路径为什么都要留**：指纹站在**现在**的磁盘上比（能发现"两个根不是
同一份"），trace 核验回看**当时**的产物（能发现"跑的时候变了"）。
那次事故**只有后者能发现**（指纹还不存在）；但若当时有指纹，指纹只会说
"两臂根不同"，**说不出漂了多少**。互补。

**固化纪律**：跑读本地仓库的实验时，语料根指向 `git worktree` 冻结快照，
**"冻结"由路径保证，不靠自律**（我自律过一次，翻车了）。

## 修补本身踩的第二个坑：指纹要测「工具读到什么」

第一版指纹哈希**磁盘字节** → 主工作区 vs worktree 快照 **53 个文件被误报**，
因为 checkout 按 `core.autocrlf` 把 LF 写成 CRLF。而工具的 `read_file` 走
`Path.read_text()`（universal newlines），两根下文本**逐字相同**
（实测 `AGENTS.md` 12,516 字节 vs 12,702 字节，都是 7,239 字符）。
⇒ 改成"归一化文本哈希 + 规模按**字符数**（与 `payload_chars` 同口径）"后，
**53 → 0 个误报**。与 v3.0「89.1% 的 default 本来就是字符串，是方言不是矛盾」
同一类教训：**先分清表示层差异，再谈内容差异**。
（附带一条 Windows 陷阱：`Path.write_text` 会把 `\n` 翻译成 `\r\n`；
测试里要构造 CRLF 必须用 `write_bytes`，否则会测出假失败。）

## 3B 臂 n=32 的正式读数（补样本的结论是「确认不了」）

| 同一份 27 工具清单 | `all7b` | `all3b` |
| --- | --- | --- |
| 关键词命中 | 7/32 | 3/32 |
| `process_ok` | 30/32 | 25/32 |
| **零工具调用** | **1** | **7** |
| 工具调用次数 | 54 | 44 |
| 非 ok 结果码 | 28/54 (51.9%) | 31/44 (70.5%) |

**配对：都中 1 / 仅7B 6 / 仅3B 2 → McNemar p = 0.2891，仍不显著。**
⇒ 补样本得到的是「**确认不了**」，不是"确认 3B 更差"。继续追该**换判据**
（精确值命中），不是加同构样本——弱判据自带噪声，加到 n=200 只是把噪声测得更准。

**2×2 交互（工具长度 × 模型）**：
- **token 侧无显著交互**：逐任务交互项 `Δ7B − Δ3B` 正 18 / 负 14，**p = 0.5966**，
  **中位交互仅 +27 token** ⇒「小模型受工具膨胀的 token 代价更大」**没有证据**。
  机制也说得通：定义是每轮固定重发的常量，两臂差 ≈ 定义长度差 × 轮数。
- **行为侧差异看得见**：3B 的工具调用数 27→4 工具时 **44 → 62（+41%）**，
  零工具调用任务 **7 → 3**；7B 是 54 → 55。⇒ 假设「清单过长让小模型更不肯查」，
  **只有两个描述性数字、未检验，只能当机制假设**。
- **对我不利的一条照写**：3B 有 **1 个任务长清单命中、短清单丢**（7B 是 0）
  ⇒ **"裁剪至少不降质量"在 3B 上没有证据支持**。

## 本机两个新坑

- **Windows 全量 `uv run pytest` 会"测试全绿但退出码 1"**：默认临时目录在
  teardown 时被本机 safe-delete 拦下（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`，
  count 632 > threshold 50）。**加 `--basetemp=<仓库外的目录>` 即恢复正常**
  （424 passed）。判别法：报告里全是点、没有 F，但退出码 1 且 dump 停在 100%。
- **`git push` 报 `CONNECT tunnel failed, response 502`**：`curl` 探
  `https://github.com` 与 `https://api.github.com` **都返回 200**，但 git 走
  proxy CONNECT 失败 ⇒ 不是网络断，是代理那一层的间歇问题。原样重试两次仍失败，
  本地提交是安全的（**已提交 ≠ 已推送**，收尾时必须回头补推）。

---

## 【交付物】findata-context-economist v0.1 落地（提交 `9135653`）

用户指令是"迅速给我开发出可用的第一版"。一天内从接口定义到有真数字的报告。

**新增**：`src/findata/economist/`（`trace.py` / `analyze.py` / `report.py` / `cli.py`）、
`scripts/context_economist.py`、console script `findata-context-economist`、
`tests/test_economist.py`（新增 29 个用例）。测试数 427 → **458**。

### 冻结批次上的三组数字（全部实测，同一份 00a95aea 语料）

| 量 | 改前（27 定义） | 改后（4 活跃） | 差 |
| --- | ---: | ---: | ---: |
| prompt tokens | 224,051 | 100,843 | **−123,208（−55.0%）**，符号检验 p=2.556e-06 |
| 工具调用数 | 52 | 53 | +1 |
| 关键词命中 | 8/32 | 12/32 | +4，McNemar **p=0.125（不显著）** |

**与 `compare_audit_arms.py` 逐个对上**（两条独立代码路径互证）⇒ 数字不是某个脚本的算术巧合。

### 四条"不做就会虚报"的设计（这版真正有价值的部分）

1. **重建必须对账**：工具返回原文不在 trace 里（§4.1 的老缺口）⇒ 用记录的参数
   重放，**字符数与记录的 `payload_chars` 完全相等**才准进字段级统计（本批 52/52，
   覆盖 100%）。对不上的退出统计，**不计分子也不计分母**。
   ⇒ 这实际上**绕开了 §4.1**：不需要改数据模型就能做字段级归因，
   代价是只能用于**确定性可重放**的工具，且必须声明"语料变了就测不了"。
2. **估算与实测分开**：字符口径估算 136,348 vs 实测 123,208 ⇒ **1.11×**。
   "字符换 token"第一次有了自己的误差量级，写进报告 §7.1。
3. **不合成单一"节省百分比"**，**不给默认单价**——两者都会让弱证据/假设像结论。
4. **分层加载的代价侧未计入**（省 payload 但可能多调一次工具）⇒ 报告 §2.2 明写。

### 提交前自查抓到的两个真缺陷（都不是测试写错）

- **报告自相矛盾**：两臂齐全时 §2.2 仍写"未实测"，而 §7 明明列着 −123,208
  ⇒ 最有价值的那个数字被自己藏起来了。CLI 控制台摘要里也有同一处硬编码。
- **金额被格式化成 0**：`round(usd, 4)` 把 $0.000041 变成 $0.0，markdown 的
  `:.4f` 同样打出 `$0.0000` ⇒ 改成**按量级选格式**（<0.01 用科学计数）。
  同批还修了 `p=0.0000`（真实值 2.6e-6 在 `:.4f` 下读起来像"恰好为零"）。
  **通用判据：固定小数位会静默抹掉真实信号，格式化必须看量级。**

### 两个静默失效（工具用法，最值得记）

- **同一条消息里对同一文件发两个 Edit，会有一个被静默丢掉**：我三次踩到
  （report.py 的 import 排序生效而删未用变量没生效、trace.py 的字段声明没生效、
  analyze.py 的返回值类型没生效），表现为后面才发现"我明明改过"。**修法：
  同一文件一次只发一个 Edit，改完立刻用 ruff/pytest 验证，不靠记忆。**
- **中文字符串里嵌套 ASCII 双引号会把整行切成 `STRING NAME STRING`（语法错）**：
  在 report.py 里一次写坏 12 处。精确定位探测器：
  `grep -P '\p{Han}"\p{Han}'`（合法代码里不会出现"汉字-引号-汉字"）。
  **修法：字符串内嵌套引号一律用「」。**
- 附带：**文档一致性门禁会拦"29 例测试"**——它被当成"全仓库测试数"的第二个声明。
  模块级计数要写成"29 个用例"，并在文档里注明**别改回去**。

### 仍未回填

`3b` 两臂的冻结读数（第 4 臂跑完即可回填）；回填后 §2.2.1 的 2×2 交互项
**要用冻结批次重算**——旧批次的 2×2 同样受语料漂移影响。

## 第四臂跑完：拿工具去跑**最弱的那一臂**，是查自己判据错误最有效的一招

3B 两臂跑完后回填 §3.5.4 与新增 §2.2.1.1（冻结四臂表 + 2×2 重算）。
**质量侧变差照原样记录**：p=0.031 → **0.125**（不显著），token 侧却是 53.4% → 55.0%。
两臂/两批次之间漂移恰好**偏向旧批次**，所以文档里写了"不许挑着引"。

### 工具在 3B 臂上自己暴露的两个缺陷（都不是"没写完"，是**判据写错**）

1. **错误返回被切成 958 个假 `path` 段。** `list_files` 报错时返回的是
   `No files under 'path: '.` 一句话，"每个非空行是一个路径"的规则把它切成了
   958 个段，值级归因再把它们当真值用，还伪造出一条 `list_files → list_files`
   的信息流（样例值就是 `path:` 本身）。
   ⇒ `segment_payload(..., result_code=...)`，非 `ok` 一律返回空表。
   **判据：报了错的结果没有"结构"可言，不猜。**
2. **产出过一条"省 0 字符"的提案。** 末轮产生的段不会再被重发（乘数=剩余轮数=0），
   拿它当候选等于给出一条**不可执行**的建议——混在可执行建议里会让整张表都不被信。
   ⇒ 剔掉，并把剔掉的数量报出来（**不是漏看，是判定**）。

### 补第 4 条杠杆（原设计漏掉的，也是 3B 臂里最大的一笔）

3B 有 **950 个路径段 / 27,769 字符 / 0 个被引用**，却因每段都短于 200 字符而
**一条提案都没产出**。⇒ 新增「返回侧·小段聚合改返回 shape」，**按组判**。
两档阈值（200 / 2,000 字符）**只决定"值不值得列出来"，不决定任何判定**；
原始数字（段数/总字符/未被引用字符）全部写进证据，供人推翻这条。
处置取"改返回 shape"而非逐条裁，理由仍是**队列长度**：900 多条小项逐条看
等于没有队列。**根因在返回结构，处置就落在返回结构上。**

### 两个数字不能混（新踩）

测试断言写成 `lever.saving_chars == ToolResult.payload_chars`，得到 `2460 != 2519`。
差的 59 字符是段间的 `"\n"` 分隔符——**它不属于任何一段，也就无从"裁掉"**。
省的是**段的字符**，payload 是**容器总量**。修法不是改代码，是把测试改成
从 `verdicts` 推导，并**顺手把差额断言出来**（`payload − 段字符 == 段数−1`），
免得将来有人把这两个数混成一个。**同类错误在本项目已出现第 3 次：
先怀疑自己的断言，再怀疑实现。**

### 收尾

- 测试数 **458 → 462**（README×2 / PITCH×5 / handoff×1，一次原子替换）。
  文档一致性门禁**真跑一次 collection** 比对，所以这个数不能"文档之间自洽"了事。
- `uv run ruff check .` 全绿；`uv run pytest` **462 passed**（含门禁），退出码 0。
- 本轮仓库内 `rm`/`grep`/`tail` 全被 WorkBuddy shim 顶掉（`safe-bin/rm` 自己还坏了，
  报 `dirname: command not found`）⇒ 清理与统计改用 `uv run python` 一行脚本；
  `pytest --basetemp` 挪到**仓库外**（`$LOCALAPPDATA/Temp/...`），
  否则 ruff 会去 lint 自己生成的临时语料（上千条 F841）。
- 提交 `b2832ed`（16 files），`git push origin HEAD:refs/heads/master` **成功**
  （`a8f7ee9..b2832ed`，exit 0）——本机 6 个本地提交已全部上线。这次先
  `git ls-remote origin master` 秒回，证明网络/凭据链正常，push 一次过。

## 提交后再自查：抓到三处，其中一处是**我自己在提交信息里报错的数**

起因很小：新提交里 3B 的产物是**加杠杆 C 之前**生成的，于是想重新生成一遍。
一重跑，三条真问题连着浮出来。

### ① 复现命令**复现不了自己**（产物缺陷）

照抄报告 §9 那条命令跑，得到的文件名是 `report-examples_context-audit.md`，
而报告自己叫 `report-frozen7b-all-vs-active.md` —— 因为**输出文件名由 `--label` 决定**
（`report-{slug}.md`），而 §9 记录的命令**没有 `--label`**。
两条独立的坑，一修修一对：
- `--label` 没记 ⇒ 文件名/标题都跑成别的；
- `--tools-file` 没记 ⇒ "下发了哪些工具"可能重建得不一样 ⇒ 定义侧提案会变。
- 还顺手踩出第三个：**Windows 路径里的反斜杠在 POSIX shell 下被当转义符吃掉**
  （`shlex.split(r'--trace C:\a\b')` → `C:ab`）⇒ 打印出来的命令**根本不能复制执行**。
  修法：新增 `_sh_arg()`，统一反斜杠→正斜杠 + 含空格加引号。
  **判据：复现段是"外部可核对"的唯一入口，它印出来的东西必须真的能跑，
  而不是看起来像一条命令。** 并加了一条**往返测试**（真跑→从 §9 抠出命令→再跑→
  断言文件集合没变）。只断言"字符串里有 --label"太弱，它和"真的能复现"不是一回事。

### ② 我自己报错了一个数（**这次值得反复看**）

前一条提交信息里我写：「3B 臂有 950 个路径段合计 **27,769** 字符」，
以及「错误返回被切成 **958 个假段**」。两条都错：

- `27,769` 是 `list_files` **13 次调用的 payload 总量**（含 8 次非成功调用与段间换行）；
  950 个段的**段字符之和是 25,960**。**容器总量 ≠ 单元量之和**（差 7%）。
- `958` 是"**950 真段 + 8 假段**"的**总数**，不是假段数。实测：13 次调用里 8 次非成功，
  旧规则下**各**切出 1 个假段 ⇒ **8 个假段 / 864 字符**。
  我把总数当成了"本次修复的收益"，**夸大了约两个数量级**。

**根因（比我原来想的更值得记）**：`958` 这个数是我从**加 gate 之前**的产物里看到的
`segs=958`（那时真段假段混在一起），修完 gate 之后真段变 950、假段变 0，
而我把"曾经的合计 958"直接读成了"假段 958"。**一个数在不同口径下含义不同，
跨口径搬运就会失真** —— 这和 §2.2 那个"容器总量当单元量"是同一族错误。

**处置**：修掉 `analyze.py` 与两处测试 docstring 里的说法，并把**正确规模**
（8 段 / 864 字符、收益小、真正的危害是伪造证据链）写进代码里；
新提交信息显式订正前一条。**错误留在 git 历史里不删** —— 面试时这条比
"我没犯过错"有用：我能说清它为什么错、怎么查出来的、以及判据是什么。

### ③ 杠杆 C 的前提是**成立的**（这条是好消息，但先前差点被误判）

发现 3B 的 `ref=0` 一度让我怀疑"950 个小段"整个是假段的产物、杠杆 C 建在假前提上。
逐条核过才是真相：950 段来自 **5 次成功调用**（25,960 字符，949 段未被引用），
是**真**的一笔浪费；8 个假段是另一回事。
**若不复核就推翻杠杆 C，会把一个对的判据当错的删掉**——"从不删规则的质检系统"
的反面同样危险：**凭一次直觉就删规则，也是没有质检**。核过再动。

### 收尾

- 两份产物用**最终代码**重新生成（§6.2 新提案 + 自带 `--label` 的 §9）。
- 测试数 462 → **463**；`ruff` 全绿；`uv run pytest` **463 passed**，退出码 0。


