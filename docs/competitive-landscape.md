# 竞品调研与项目价值判断

> 触发：用户质疑「这个项目是精致的假设，还是真实价值」「用途太窄是一种自嗨」，
> 要求做竞品调研，必要时仿照主流竞品重做。
> 调查日期：2026-09-18。数据来源分两档，文中逐条标注。

---

## 0. 一句话结论

**命题是真的，位置是窄的——而「窄」的判断比我原来想的更严重，
因为我要解决的问题在 2026 年下半年已经有 4 个 npm 包 + 3 篇论文在做，
而我的产物形态（一份报告）决定了它没有闭环消费者。**

这不是「假问题」，是「真问题 + 错位置 + 弱产物形态」。
修法不是把项目做大，是**把它从「离线质检器」搬到「运行时必经环节」**。

---

## 1. 竞品全景：四层夹心

我的项目站在中间。上面有大厂，同层有 npm 包，旁边有学术项目，下面有评测框架。

### 第 1 层｜训练数据清洗大厂（我原来的"想对标"对象）

| 项目 | ⭐ | 做什么 | 与我重叠 |
|---|---|---|---|
| cleanlab | **11.6k** | 标签错误检测，data-centric AI，5 万+ 项目使用 | 无（要 pred_probs，得有模型） |
| Data-Juicer（阿里通义） | **6.5k** | 200+ 算子，多模态，Ray 分布式，YAML recipe，**含 agent data preparation** | 高（它有 agent 数据算子） |
| Docta-ai/docta | 3.5k | "A Doctor for your data"，含 rlhf 标签 | 中 |
| datatrove（HuggingFace） | 2.8k | 大规模文本去重过滤 | 低 |
| NVIDIA NeMo Curator | **1.8k** | GPU 加速去重，数据配方 | 中 |
| data-prep-kit | 892 | 预处理工具集 | 低 |

> 数据来源：GitHub topics/data-curation 页面 + awesome-repositories 榜（**均为直接读数**）
> Data-Juicer 星数两个来源不一致（5.8k / 6.5k），取高档并标注。

**含义**：这一层我打不过。不是能力问题，是**它们有分布式引擎、有 200 个算子、有大厂维护**。
我的 21 个 schema 缺陷发现，对它们来说是"一个算子"的量级。

### 第 2 层｜工具定义静态质检（**我的直接竞品，2026 年新出现**）

| 项目 | 形态 | 规则数 | 我原来没有的能力 |
|---|---|---|---|
| `mcp-tool-lint` | npm | 6 类 | 引用论文背书；CLI + 编程 API |
| `mcp-schema-lint` | npm | 15 条 | **4 个 severity preset**、自定义规则、零依赖 |
| `mcp-lint` | npm | 多类 | **跨客户端兼容矩阵**（Claude/Cursor/Gemini/OpenAI…）、`diff` 做 CI 回归、**auto-fix**、直连活服务器、0-100 评分 |
| `mcp_tool_description_validator` | GitHub | **46 条** | **成熟度评分 0-100**、SARIF 报告、HTTP 服务、CI mode |

**规则重合度极高**，几乎一一对应：

| 我的规则 | 它们的规则名 |
|---|---|
| 工具描述缺失/过短 | `description-length` |
| 参数无描述 | `require-param-descriptions` |
| 描述动词含糊 | `no-vague-verbs` |
| **`required` 字段缺失 → 我用"未标 default 即必填"推断** | **`require-required-array`** |
| 工具重名 | `no-duplicate-names` |

> `mcp-tool-lint` 原文：「A February 2026 research paper analyzing 1,899 MCP tools from
> 200 servers found that **97.1% have at least one quality defect**」
> 注意：它的分母是 1899 个 MCP 工具，规则比我宽（"描述短于 20 字符"就算）；
> 我的分母是 60000 条样本 / 3605 个工具名，命中 2.25%。**两个数字不可直接比较。**

**含义**：我在「发现 schema 缺陷」这件事上**不是第一个，也不是最好的**。
它们工程化程度全面超过我：我给一份报告，它们能给 SARIF、能 auto-fix、能做 CI diff、
能算跨客户端兼容矩阵。**我领先的只剩归因深度**（见 §4）。

### 第 3 层｜Agent 失败归因与运行时干预（**这是热度真正所在**）

| 项目 | 时间 | 机制 | 公布效应量 |
|---|---|---|---|
| **Outcome Monitors**（南密西西比大学） | 2026 | 拦截工具**返回值**异常 → 附加**非阻塞诊断回执 + 恢复工具建议**，不改执行不阻断 | **ToolMaze 10.9% → 28.1%**；τ-bench retail **+14.0 / +12.0 分** |
| **AgentDebugX**（arXiv 2607.18754） | 2026-07 | Detect→Attribute→Recover→Rerun 闭环，**19 类失败模式**，DeepDebug 多轮根因诊断 | **GAIA 55.8% → 63.6%**（73 个失败修复 13 个）；Who&When strict 归因 28.8% vs 单遍 21.7% |
| **AgentTether**（arXiv 2607.06273） | 2026-07 | Critical Transition Graph + 双异常检测；运行时 **Check–Decide–Inject** | 未取到独立数字 |
| **Schema First Tool APIs**（arXiv 2603.13404） | 2026-03 | 受控实验：自由文档 vs JSON Schema vs **Schema + 结构化诊断** | 「schema 条件**减少 interface misuse 但不减少 semantic misuse**」 |

**Outcome Monitors 那句话是关键**：
> 「pairing diagnostic receipts with actionable recovery affordances **drives the bulk of these gains**」
> （诊断回执 + 可执行恢复选项，是收益的主要来源）

这**直接回答了我项目最大的空洞**——Q37 里我承认"模型指标提升给不出"。
现在有了：**效应量是 10.9%→28.1%，接近 3 倍。这是本机 RTX 4060 跑 7B 也能测出来的量级**，
因为它不是 2% 的抖动，是失败→成功的跃迁。

**Schema First Tool APIs 的结论是我项目核心命题的学术版本**：
「interface misuse」（参数不符合契约）vs「semantic misuse」（schema 合法但没意义）
—— 这正是我说的**「我认不出参数」vs「参数本身是错的」**，**形态相同、结论相反**。

### 第 4 层｜评测框架（我原以为的"邻域"）

| 项目 | ⭐ | 说明 |
|---|---|---|
| promptfoo | 23.7k | 提示与模型对比测试 + 红队 |
| OpenAI Evals | 19k | 评测框架与开放注册表 |
| DeepEval | 17.2k | LLM 应用单元测试，50+ 指标，pytest 集成 |
| Ragas | 15k | RAG 专用评测 |
| Arize Phoenix | 10.8k | 可观测 + 评测，跑在 trace 上 |
| garak | 8.6k | NVIDIA 漏洞扫描 |
| **agentevals（LangChain 官方）** | **668** | **轨迹评测：tool-call 严格匹配（strict/unordered/subset/superset 四模式）+ LLM-as-judge** |

**这行数据的信息量最大**：`agentevals` 是 LangChain 亲自出手做的 agent 轨迹评测，
有 LangSmith 商业后盾、有官方文档、有两个包（agentevals + openevals）的系统设计——
**只有 668 颗星**。

对比同一份榜单：promptfoo 23.7k、deepval 17.2k。**同一个"评测"大类里，
"agent 轨迹"这个细分比通用评测小一个数量级。**

**含义**：不是我的切入点小，是**「数据/评测质量」这个整体赛道都小**。
再看框架层：LangChain 142k、superpowers 257k、hermes-agent 217k（**二手数据**，来自
第三方追踪站）——**agent 框架比数据质量工具大两个数量级。**

**所有人都在造 agent，很少有人愿意回头清理数据。** 这就是"窄"的结构性原因。

---

## 2. 我的项目现在站在哪

```
        ┌─────────────── 大厂（Data-Juicer 6.5k / NeMo Curator 1.8k）───────────────┐
        │                     通用清洗，200 算子，分布式，我打不过                    │
        └──────────────────────────────────────────────────────────────────────┘
                                          ▲
        ┌──── npm 包（mcp-tool-lint / mcp-schema-lint / mcp-lint / validator）────┐
   ┌────┤            静态质检，规则几乎一样，工程化全面超过我（SARIF/auto-fix）    ├────┐
   │    └──────────────────────────────────────────────────────────────────┘    │
   │                              ▲                                             │
   │                    【我现在在这里】                                          │
   │        离线批处理 · 一份报告 · 21 个缺陷 · 命中 1351 / 60000                │
   │        独一无二的东西：值错位归因、证据强度分层、默认不丢样本                  │
   │                              ▲                                             │
   │    ┌──────────────────────────────────────────────────────────────────┐   │
   └────┤  学术侧（Outcome Monitors / AgentDebugX / AgentTether）            ├───┘
        │  运行时归因 + 自修正，效应量 10.9%→28.1%、55.8%→63.6%             │
        │  它们做运行时，但都不做「归因到 schema 缺陷」                        │
        └──────────────────────────────────────────────────────────────────┘
```

**夹在中间，且是最窄的那一格。**

---

## 3. 判断：命题真、位置窄、产物形态弱

分三档说，每档标注证据强度。

### 3.1 命题是真的（**强证据**）

| 证据 | 来源 | 强度 |
|---|---|---|
| 2 处 `default` 值错位（`permitivity` 258 次 / `mean_confidence_interval.confidence` 83 次） | 我自己全量核 | **硬**：值在、但贴在 `type: int` 的别的参数上，疏忽解释不通 |
| 1,899 个 MCP 工具中 97.1% 至少一个缺陷 | 2026-02 论文（经 mcp-tool-lint 引用） | 中（二手转述，未读原文） |
| GPT-4o 在 τ-bench retail pass@1 仅 44%，**最大失败类别是参数构造而非工具选择** | tau-bench（ServiceNow Research, 2025） | 中（搜索结果转述） |
| Phi-4 14B 用原始 JSON schema 在 20 工具下准确率 **0%**，编译后 **84.4%** | TSCG 论文 | 中（转述） |
| 12 个模型 × 30 种 JSON Schema 约束：错误率 15%→3%，同工具跨模型成功率差 5 倍 | Mastra 实测（掘金转述） | 弱（二手） |

**结论**：**「工具定义的缺陷会导致 agent 调用失败」是真问题，且已被学术界量化。**
这一关过了——它不是精致的假设。

### 3.2 位置是窄的（**强证据，且用户的判断是对的**）

三条独立证据：

1. **同层竞品密度**：4 个 npm 包在同一个点上，规则与我重合 5/5。
   → 说明这个点**已经被认领**，而且是 2026 年集中认领的。
2. **赛道天花板**：`agentevals`（LangChain 官方、有商业后盾）只有 **668 ⭐**，
   而通用评测 promptfoo 是 23.7k。→ **这个细分的人群天花板可见。**
3. **人群定义**：mcp-lint 面向「写 MCP server 的人」（= 所有 agent 开发者）；
   我面向「拿到一批轨迹要决定喂不喂训练的人」（= 做 SFT 数据的人）。
   → **后者是前者的一个子集，可能不到十分之一。**

### 3.3 产物形态是弱的（**这一条是我原来没看到的，也是"自嗨"判断的准确落点**）

用户说「自嗨」。我原来会反驳。**现在我认为这个判断在这一点上准确：**

> **我的产物是「一份报告 + 一批标签」。如果没有人拿这批标签去训练，
> 这份报告的价值就是 0。**

而我在 Q37 已经自己承认了：**「脏率给得出，模型指标提升给不出」**。
把这两句放在一起——**产物没有闭环消费者**——这就是自嗨的技术定义。

**补法不是"更有力地论证它有价值"，是给它一个必须消费它的下游。**

---

## 4. 我的独特缝隙（我没有被覆盖的地方）

把上面三层的**数据流方向**画出来就清楚了：

| 项目 | 输入 | 输出 | 数据流方向 |
|---|---|---|---|
| mcp-lint 们 | 静态 tool 定义 | "你的定义有 3 个 warn" | **静态 → 定义方** |
| Outcome Monitors | 工具**返回值** | "违反了不变式 X，建议用工具 Y 恢复" | **运行时 → 模型** |
| AgentDebugX | **完整轨迹** | "第 3 步是决定性步骤"（19 类 trajectory 失败模式） | **事后 → 开发者** |
| **缺口** | **运行时那次失败** | **"这次失败归因到 tool schema 的哪一处缺陷 + 对 schema 的可执行修复"** | **运行时 → 定义方** |

**没有人把「运行时失败」反向归因到「工具定义缺陷」上。**

- Outcome Monitors 查 **outcome contract**（返回值合不合法），不查定义本身写错了没有
- AgentDebugX 的 19 类 taxonomy 是**轨迹失败模式**（规划错 / 记忆陈旧 / 交接错），
  不是 **schema 缺陷模式**（值错位 / 缺声明 / 契约自相矛盾）
- mcp-lint 查定义缺陷，但**是开发期静态的，不与真实失败关联**——
  它不知道哪条 warn 真的害了 agent

**而我项目最硬的那块能力恰好在这里**：「在形态相同的事实之间（模型漏填 vs 定义缺陷）
做出区分」——这正是 v1「停牌 vs 漏采」命题的运行时版本，
而且**它把第 2 层（静态 linter）和第 3 层（运行时监控）连起来了，两边都没连。**

> 这条缝隙的可靠性：**中**。我基于 4 次搜索覆盖了 GitHub/npm/arXiv 的主要项目，
> 但不能排除有我没搜到的实现（尤其是不发论文的创业公司）。

---

## 5. 三条转型方向

### 方向 A｜Tool-Call Trust Gate：工具调用的运行时归因与自修正层 ⭐推荐

**形态**：一次 tool call → 核验 → 归因（模型错 / 定义错 / 环境错）→ 注入诊断回执 → agent 自修正。

```
现在：一批轨迹 ──[离线质检]──> 一份报告            （谁看？）
改成：一次调用 ──[运行时核验+归因]──> 诊断回执 ──> agent 当场修正（效果当场可测）
```

| 维度 | 评估 |
|---|---|
| 人群 | **所有跑 agent 的人**（不是只有做 SFT 的）——比现状大一个数量级 |
| 频率 | **每次 tool call 都过**（不是一次性报告）——这是"热度"的结构性来源 |
| 效应量 | **可测且大**：对标 Outcome Monitors 的 10.9%→28.1%，本机 RTX 4060 + qwen2.5:7b 够用 |
| 代码复用 | **约 70%**：`agentops/`（schema 归因）、`agent/`（LangGraph 编排）、`mcp/server.py`、`observability/` 全部可用 |
| 新增工作 | 拦截器（MCP middleware 或 SDK monkey-patch）+ 诊断回执格式 + 对照实验 |
| 可对标 benchmark | **τ-bench（公开）**、ToolMaze、Who&When、GAIA |
| 学术锚点 | 三篇 2026 论文（都是"运行时"方向，说明正在爆发） |
| 差异化 | 见 §4——唯一做「运行时失败 → 定义缺陷」反向归因的 |
| 风险 | τ-bench 需要真实 API 或本地模型；效应量可能小于论文（不同 benchmark） |

**为什么它能替掉"自嗨"判断**：诊断回执**被 agent 当场消费**，
修正效果**当场可测**——产物第一次有了闭环消费者。

### 方向 B｜MCP Tool Quality Gate：直接进 MCP 生态抢分发

在 mcp-lint 的战场上打，差异化 = **「从真实失败轨迹反推 schema 该改哪里」**
（别人的 linter 只有静态规则，我有"这条 warn 真的害了多少次调用"）。

- 优势：人群大（所有 MCP 开发者）+ npm 是现成分发渠道 + 可以真的发出去给别人用
- 劣势：**4 个竞品已占位**，且要写 TypeScript（我主体是 Python）
- 判定：**如果目标是"有外部用户"，这条最直接；但如果目标是"面试作品"，回报率不如 A**

### 方向 C｜把能力做成 Data-Juicer 的算子（挂靠，不对抗）

不做独立项目，把我 21 个 schema 缺陷探针做成 Data-Juicer 的一个 operator，
跟 200 个算子并列。

- 优势：**借大厂生态的分发**，不用自己从零做引擎；"给知名开源项目提交算子"是强简历项
- 劣势：**主线上你不是主角**，项目叙事变成"我贡献了一个算子"
- 判定：适合作为**方向 A 的副产品**（顺手做），不适合作为主线

---

## 6. 推荐与最小验证闭环

**推荐方向 A**，并保留现状作为它的「数据侧 mode」（不删除，降级为一个入口）。

理由：它一次修掉三个问题——① 人群窄 → 所有 agent 开发者；
② 产物无消费者 → 诊断回执被当场消费；③ 下游价值证不出 → 可测的大效应量。

**两周最小闭环（每步都可独立验证）**：

| 步 | 做什么 | 验收标准 |
|---|---|---|
| 1 | 把 `agentops` 的 schema 归因能力包成**运行时拦截器**（先支持 MCP 一种协议） | 一个坏 schema 的工具被调用时，能否输出「归因到该缺陷」而非泛泛报错 |
| 2 | 定义**诊断回执**格式（违反了什么 + 这是谁的责任 + 建议怎么改 + 可用的替代调用） | 回执能让一个 7B 模型在下一轮改对，而不是需要人工读 |
| 3 | **对照实验**：注入诊断 vs 不注入，在 τ-bench retail（或自建 30 任务集）上比 | **必须有数字**，且要报告效应量与样本量；若效应量不显著，如实写"不显著" |
| 4 | 用公开 benchmark 替换掉自造的 held-out 63.6% | 去掉那个"红灯不划掉"的尴尬 |

**第 3 步是全部的关键**：它是这个项目能不能从"自嗨"变成"有据"的分水岭。
如果做出来，我就能回答面试官那个"下游价值在哪"——**用数字**。

---

## 7. 诚实边界（哪些是我核过的、哪些是推断的）

**我亲自核过的**：
- 自己全量跑的 60000 条 / 命中 1351 / 21 个缺陷 / 2 处值错位
- 上述所有 ⭐ 数字来自 GitHub topics 页、awesome 榜、ai-tldr 对比表的**直接读数**

**搜索转述、未读原文的**（用之前需要核实）：
- 97.1% / 1899 工具（经 mcp-tool-lint 引用）
- GPT-4o τ-bench 44%、TSCG 0%→84.4%、Mastra 15%→3%
- Outcome Monitors 10.9%→28.1%、+14/+12 分
- AgentDebugX GAIA 55.8%→63.6%、Who&When 28.8%
- superpowers 257k / hermes-agent 217k / LangChain 142k（第三方追踪站，非 GitHub 直读）

**我明确没做到的**：
- 没有读完那三篇论文的原文（只有摘要与解读页）
- 没有验证 mcp-lint 们的真实用户量（npm 下载量未查）
- 不能排除有未公开发布的竞品
- **方向 A 的效应量在本机能否复现，尚未验证**——这是最大的未知，也是第 3 步要先做的事

---

## 8. 补录：用户澄清「我想做的是一个 agent 项目」后的再调研

**用户澄清**：目标仍是面向实习，但**想做的是一个 agent 项目**。
→ **这直接否掉了我上面 §5 的三个方向**：A 是 middleware、B 是静态检查器、
C 是一个函数。**没有一个的核心价值由 LLM 决策提供。**

这也印证了上一轮拷问的第一句质疑（「你到底是写了个规则脚本，还是真的在做
质检系统」）——**根因是：我项目的核心价值是规则提供的，LLM 只在
`llm_triage.py` 里做辅助。包装出来的"agent 项目"经不起追问。**

### 8.1 新证据：self-healing 已是行业热词，但缺口正好在我这里

| 来源 | 给出的东西 | 缺什么 |
|---|---|---|
| **Arize**（self-healing agents 词条） | **三类失败分类**（transient / agent misuse / successful-but-wrong）+ 「recovery 必须匹配 failure class」+「self-healing hides the thing it fixed」 | 给出处置「**then a tighter schema or a clearer tool description**」，但**没说怎么知道该改哪里、改哪一处、改完怎么验证** |
| **appscale.blog**（2026 production pattern） | **repair loop** 完整形态：Pydantic 校验 → 失败时回注**纯英文校验错误**（不是 raw Pydantic JSON）→ 一次修复机会 → 二次失败 hard-fail 并**入离线 eval set 作回归候选**；成本只增一轮 800-1200ms | 指标只到**工具粒度**（`repair-rate per tool` <5% 健康 / >15% 说明 schema 有歧义），**没有参数粒度** |
| **devsatva**（企业案例） | **detector / diagnostician / healer 三角色** + LangGraph + 人在回路；**schema drift 占事件 28-41%（#1）**；MTTR **4h12m → 9min**、升级 -81%、87% 自动解决 | 面向 Kafka/Flink/dbt 管道，不是 tool contract |
| **OTel GenAI conventions** | **2026 已 stable**：`gen_ai.tool.call` span + `tool.name` / `call_id` / **`args_hash`**（sha256，记原文泄 PII）/ `result_code` / `retry_count` / `duration_ms` | — |
| **LLM-AutoDP**（VLDB 2026-05） | **LLM as agent 自动生成并优化数据处理策略**，反馈信号 + 对比评估迭代；**>80% win rate** vs 未处理、~65% vs AutoML 基线、搜索时间省 10× | 医疗数据集领域；是"策略搜索"，不是"契约修复" |
| Meta Autodata（2026，**仅论坛转述未验证**） | 闭环多智能体：专家 agent 提取/重写/增强 + 模型性能反馈定向补弱项 + 知识去噪；数据集缩到 1/10 性能持平 | 无法核实 |

**必须承认**：self-healing **不是空白**——Arize 有词条、已有 Agent Skill 形态实现
（`npx skills add ... --skill self-healing-agents`）、有企业落地案例。
所以下面讲的是**差异化**，不是"没人做"。

### 8.2 我的精确位置（一句话）

把三家的输入输出排开：

| | 输入 | 处置动作 | 粒度 |
|---|---|---|---|
| Arize | 失败类型 | 重试 / corrected retry / 换模型 | 失败类 |
| appscale | 校验错误 | 回注错误让模型自修 + **收紧 schema** | 工具 |
| devsatva | 管道事件 | rollback / re-ingest / patch dbt | 管道 |
| mcp-lint | 静态定义 | 报 warn/error | 规则 |
| **缺口** | **真实失败 + 工具定义** | **改哪一处参数 + 补丁 + 恢复数** | **参数** |

> **别人治的是 agent 的病；我治的是工具契约的病——而且我能证明治好了。**
> 三家的表述都是"then tighten the schema"，**没有人能说出"改哪一个参数的哪一行，
> 影响多少条调用，改完恢复多少"。**

**而这恰好是我项目最硬的能力**（值错位归因、证据强度分层、默认不丢样本），
也是它唯一不可能被规则 linter 复制的地方。

### 8.3 四个「真正是 agent」的方向

判据：**核心价值必须由 LLM 的多步决策提供，规则降级为 agent 手里的工具。**

#### 方向 D｜Tool Contract Steward（工具契约自治 Agent）⭐ 推荐

**五步，其中"查什么/怎么归因/怎么修"由 LLM 决策：**

```
1 Detect    接入 agent 的 trace（OTel gen_ai.tool.call span）→ 找失败信号
            （不是静态规则扫，是从真实调用里看出来的）
2 Attribute 归因到「哪个工具 · 哪个参数 · 哪一类缺陷」
            ← 我的核心能力，规则做工具，LLM 做裁决
3 Propose   生成对 schema/description 的修复补丁（不是对轨迹的）
4 Verify    在沙箱里用修好的 schema 重放失败样本 → 给出恢复数
5 Report    出补丁 + 证据链 + 影响面（"改 1 处，258 条恢复"）+ 可选 PR
```

| 维度 | 评估 |
|---|---|
| 是不是真 agent | **是**：多步规划 + 工具调用（探针/沙箱/重放都是它的工具）+ 自主决策查什么 + 自我验证 + 失败可重规划 |
| 大众程度 | 所有部署 agent 的人 + 所有维护 tool catalog 的人 |
| 复用率 | **最高**：`agent/`（LangGraph 骨架）、`agentops/`（探针+归因）、`agentops/sandbox.py`（验证）、`observability/`（OTel，**可对齐已成 stable 的 GenAI 语义约定**）、`mcp/server.py`、`eval/` |
| **可测性** | **最好：不需要 GPU、不需要训练模型**。验证修复是确定性的——改完 schema 重放，**恢复数就是指标**。这直接补掉我最大空洞（Q37「模型指标提升给不出」） |
| 指标 | 归因准确率（对小样本人工标注）+ **修复恢复率** + repair-rate per tool 的下降 |
| 差异化 | §8.2——唯一做到参数粒度 + 可证明的修复 |
| 风险 | 需要 agent 侧的真实 trace（可先用 xlam 的调用记录模拟 + 自建一个带 OTel 的 demo agent） |

#### 方向 E｜Trajectory Curator Agent（轨迹策展 Agent）——改动最小的正统战法

保留现有主线，但把 **LLM 从辅助提到主决策**：agent 自己看轨迹、自己判能不能喂
训练、自己写理由，并与规则归因**对赌**。

- 是真 agent：LLM 逐条决策 + 可追问 + 可自修正
- 复用率：**极高**（几乎全是现有代码，把 `llm_triage` 提到主体）
- **评测设计天然成立**：agent vs 规则的**一致性 κ** + agent 抓到而规则抓不到的（新增覆盖）
- 优势：**直接延续现有主线**，不需要新建架构；且"两个归因器对赌"本身就是可测的
- 劣势：**产物仍是标签（闭环消费者弱）**；且 CLEANER 等已做自净化轨迹

#### 方向 F｜Data Pipeline Incident Agent（数据管道事故 Agent）

detector / diagnostician / healer 三角色，对标 devsatva 的 MTTR 4h12m→9min。

- 大众程度：数据工程，人群大
- 复用：A 股领域包 + 探针可复用
- **劣势**：Kafka / Flink / dbt 我都没有，要新建大部分；且这层已有厂商产品

#### 方向 G｜MCP Server Autopsy Agent（MCP 服务器活体质检 Agent）

方向 D 的特化入口：agent 连上一个 MCP server（stdio/SSE）→ 拿工具列表 →
**真的去调用工具试探边界** → 归因 → 出补丁。

- 分发渠道最清晰（MCP 生态 + npm，2026 高频词）
- 与 mcp-lint 的差别：它是**静态 lint**（即使 `--server` 也是把活服务器的定义拿来静态查），
  **没有"主动调用去试探"**——这条我核过，是它自家文档里写的
- 判定：**适合作为 D 的第一个入口**，不适合独立成线

### 8.4 推荐

**D 为主线，用 G 做入口，E 的"agent vs 规则对赌"做评测设计。**

一条能同时满足三件事的路线：**它既是真 agent，又保留了我唯一不可替代的能力
（参数粒度的契约归因 + 可证明的修复），而且指标不需要 GPU 就能算出来。**

最小闭环（可 2 周完成，每步独立可验收）：

| 步 | 做什么 | 验收 |
|---|---|---|
| 1 | 一个最小 demo agent（3-5 个工具，故意有 1 处值错位缺陷），跑 50 轮，用 OTel `gen_ai.tool.call` 记 span | trace 里能看到 `validation_failed` + `retry_count` |
| 2 | Steward agent 读 trace → 归因到「工具·参数·缺陷类」 | 归因结果对照人工标注，报准确率与样本量 |
| 3 | 生成补丁 → 沙箱重放 → **报恢复数** | **这是全项目第一个"闭环消费者"指标** |
| 4 | 对照：只用 appscale 式"回注校验错误" vs 我的"回注错误 + 契约补丁" | 两组恢复率对比；**不显著就如实写不显著** |

---

## 9. 需求侧再调研：按「谁需要」而不是「我有什么」重选方向

**用户给出的选择标准（原话）**：
> 「尽量是一个**通用 agent**、**受众面足够广**的，
> **面向需求而不是面向已有的技术轨迹**决定下一步开发」

### 9.1 先承认 §8 推荐方式的错误

§8 我推荐方向 D 的论据是「**复用率 70%**」和「**我唯一不可替代的能力**」。
**这是技术轨迹驱动**——它回答「我能做什么」，不回答「谁需要什么」。

更严重的是：**方向 D 违反了我自己立的判据。** 我的项目铁律写着处置要看
「**①能不能给出可执行动作**」。而现实中大多数 agent 开发者的工具定义来自
**第三方 MCP server（4,133 个 server / 11,393 个工具）**——**他们改不了**。
我产出的"schema 补丁"对他们就是**不可执行的动作**，正是我在质检里判定为
"没有队列价值"的那一类处置。**我用自己做质检的判据一测，自己的方向就没过。**

### 9.2 需求侧硬数字（全部外部来源）

**成本**（人群 = 所有跑生产 agent 的人，含只关心钱的老板）：

| 数字 | 来源 |
| --- | --- |
| agentic 每任务 token 成本 = 普通 chatbot 的 **5–30 倍** | Gartner 2026-03 |
| **73%** 的组织超出原始 AI 预算 | FinOps Foundation 2026 |
| **重复发送的上下文占账单约 62%**（单一最大优化目标） | Vantage 2026 / Iternal 审计 30 团队（两处独立测出同值） |
| 月 AI 支出 **+36% → 约 $85,500**；>$100k/月 的组织 **20% → 45%** | CloudZero（500 位工程负责人） |
| **>40% 的 agentic 项目将在 2027 年底前被取消**，首因是成本 | Gartner 2025-06 |
| LangChain 多智能体无限循环 **11 天烧 $47,000** | 2026 广为引用的事故 |
| 编码 agent **$2,000/工程师/月**；单任务 1–3.5M token | 行业审计 |
| 前缀缓存：Anthropic 约 **10%** 基准价、OpenAI **50%** | provider 定价 |

**工具膨胀**（同一个人群，同一个痛点的另一面）：

| 数字 | 含义 |
| --- | --- |
| **50 工具 → 84-95%**；**200 工具 → 41-83%**；**740 工具 → 接近 0** | 选择准确率随工具数崩塌 |
| 列表中间 40-60% 位置 → **22-52%**，两端 31-32% | Lost in the Middle |
| **207 → 417 工具**时非线性断崖 | 不是平滑退化 |
| **MCP 工具元数据占上下文窗口 40-50%** | 直接开销 |
| 12 → 90 工具，准确率 **90 出头 → 70 出头**；藏 75 个后**反而更聪明** | 真实案例（agent-swarm） |
| **4,133 MCP server / 11,393 agent 工具**；实际只用 **10-15%** | 生态规模 vs 使用率 |

### 9.3 现有解法的共同缺口（差异化在这里）

| 手段 | 决策依据 | 缺 |
| --- | --- | --- |
| 模型路由（RouteLLM，省 40-80%） | **按任务类别预设** | 与"这个具体返回该不该进上下文"无关 |
| 前缀缓存（provider 原生） | **按 token 前缀匹配** | 同上 |
| 工具检索（RAG-MCP / AutoTool，13.62%→43.13%） | **按 query 与工具描述的向量相似度** | 相似 ≠ 会被用；不看历史使用 |
| 上下文压缩（滚动摘要） | **按位置/长度** | 不看内容是否真被用到 |
| 成本面板（Langfuse / LangSmith） | **被动统计** | 不主动给出该改什么 |

> **全部是预置型/规则型决策。没有一个是从「模型在这次真实调用里实际用了什么」
> 反推该保留什么**——因为这件事**只能从 trace 观测，不能从文档推出来**。
> 而这正是本项目最硬的能力：**从真实证据出发做准入决策，不凭想象设口径。**

### 9.4 结论：转向「准入决策」的第三个域

**本项目三版主线是同一个动作换域：**

| 版本 | 问题 | 受众 |
| --- | --- | --- |
| v1 可信数据引擎 | 这个数字**能不能引用** | 数据分析师 |
| v3.0 训练数据质检 | 这条轨迹**能不能喂训练** | 做 SFT 数据的人（窄） |
| **v4.0 上下文策展** | 这段内容**该不该进上下文** | **所有跑 agent 的人**（最广） |

核心命题「形态相同、结论相反」的**第四次变体**：
一段 5,000 token 的工具返回，**看起来信息丰富（形态），实际模型只用了 3 个字段（结论）**
——看起来该留，实际该裁。判据仍是 **加信息可逆、删信息不可逆**。

**v4.0 的不可替代性质**：验证是**确定性数字**（token / 成本 / 成功率），
**不需要 GPU、不需要训练模型、不需要人工标注**——这彻底补掉本项目最大的空洞
（Q37「模型指标提升给不出」）。

**已写入 `docs/ROADMAP.md` 顶部（v4.0 节）**，含 R5.0–R5.3 验收门禁与三条诚实边界
（其中最重要：token 侧结论可迁移、**成功率侧只在本地 7B 口径下成立**）。

### 9.5 本条结论的证据强度

- **强**：上表所有数字均来自搜索到的外部来源（行业报告 / 论文 / 工程博客），
  但**均为转述，未读原始报告**
- **强**：现有解法的决策依据（路由按类别 / 缓存按前缀 / 检索按相似度）来自各自的
  公开文档与论文摘要，属于机制描述，不易转述失真
- **中**：「从 trace 反推保留什么」这条缝隙没被占——覆盖了 GitHub/npm/arXiv，
  不排除有未发布的内部实现（Uber 内部已有近似做法，但未开源）
- **未验证**：v4.0 在本机能省多少 token、成功率是否真的不降——**这正是 R5.2 要做的**
