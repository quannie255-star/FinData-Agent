# 上下文经济学报告 · examples/context-audit

> 本报告由 findata-context-economist 从 trace 自动生成。文中「实测」指 trace 里原样记录的 provider usage 或本项目数的确定性字符数；「换算」指由实测比值推出的估算值；「未测得」指本次输入不具备测它的条件。
> 生成时间：2026-09-22 12:24:35

## 1. 本报告读的是什么

| 项 | 值 |
| --- | --- |
| runs 增强层 | `examples/context-audit/runs-all3b-fx.jsonl` |
| OTel span 层 | `examples/context-audit/spans-all3b-fx.jsonl` |
| 任务数 | 32 |
| 工具调用 | 44 次 |
| 模型调用轮数 | 73 |
| 模型 | qwen2.5:3b |
| 语义约定 | `gen-ai-stable-2026` |
| 工具清单识别 | tools_chars 完全匹配（11559 字符）⇒ 本批带的是「all」清单 |

### 1.1 输入层的能力边界（来自数据层，不是报告作者补的）

- span 之间**没有 run 级父 span**（44 个工具 span 对应 44 个不同 trace_id）⇒ 只用 span 无法把调用归到具体任务。这是**被观测系统**的打点缺口，不是读法问题；本工具需要 `gen_ai.invoke_agent`（或等价）级父 span 才能做逐任务归因
- trace 里不含**系统提示**与模型的中间推理文本 ⇒ 重建的 `before` 缺系统提示（可能把「本就在系统提示里」的值误判成新信息），`after` 只含最终答复与后续调用参数（**会漏判**引用，方向单一）
- 有 13 / 44 次调用带**原生字段记录**（R5.1）⇒ 这部分调用的字段级归因**不依赖重放**，对第三方 MCP 工具同样成立；其余调用仍走重放+对账路线

### 1.2 重建对账（字段级归因的准入闸门）

用 trace 里记录的参数重放工具，语料根 `C:\Users\10393\WorkBuddy\Worktrees\FinData-Agent\ctxbench-frozen-noleak`。共 44 次调用：**31 次重放字符数与记录值完全相等**（覆盖率 70.5%），0 次不一致、0 次参数不可解析、0 次无语料根。

**不一致的调用一律退出字段级统计**（不计分子也不计分母）——重建出来的内容与当时返回的内容不是同一份，基于它做的归因没有意义。

### 1.3 字段级归因走的是哪条路（决定这套方法能给谁用）

原生记录（调用发生时记下字段，**不需要重放**）：13 次调用；事后重放：31 次调用。另跳过 13 次**已带原生记录**的调用（不重放）。

**这两条路不能混报**：原生记录对第三方 MCP server 的工具同样成立（它们可能有副作用、非确定性、要鉴权，重放跑不起来）；重放只对本项目的确定性本地只读成立。只有原生那条路能给别人用。

## 2. 三组数字

### 2.1 换算基准（本批实测，不是查表常数）

从本批 73 个模型调用实测：`prompt_tokens / (messages_chars + tools_chars)` 合计 0.1948、中位 0.1936 token/字符（p10–p90 = 0.1900–0.1959）。

分子是 provider usage（实测），分母是本项目数的字符（确定性）。**用这个系数系列算出来的 token 是估算值**；p10–p90 的宽度就是它不可信的幅度。

### 2.2 省下的 token

- **实测**：本次 trace 未做改后重跑 ⇒ 节省量**未实测**。
- **换算（上界式估算）**：各提案字符口径节省合计 621,620 字符 ⇒ 约 121,075 prompt tokens（按 2.1 的 0.1948 token/字符）。

这个估算是**上界**，两个方向的偏差同时存在，而且方向相反：
  - 偏高：段级「未观测到引用」受 `after` 缺中间推理文本影响，是**漏判** ⇒ 被判为可省的段里混有实际被用到的内容；
  - 偏低：只算了「工具定义」与「工具返回」两项，**没有**算历史消息重发与系统提示的优化空间。

**各提案不可简单相加**：返回侧的两条提案可能作用于同一段字符（重复调用的返回既进「分层加载」也进「结果复用」）⇒ 相加会重复计算。分项数字见 §6，那里每条只算自己那一段。

**还缺一侧代价（重要）**：返回侧的「分层加载」省的是 payload 字符，但模型拿不到全文后**可能要多调一次工具**（先取索引、再取正文）——这一侧的成本**没有计入**上面的节省量。真实净收益必须由改后重跑给出。

### 2.3 折算成本

- 单价：**$0.4 / 百万 prompt token**（由调用方提供，本工具不猜）
- 换算：121,075 tokens × $0.4/M = **$0.0484 / 这 32 个任务**
- 该金额继承 2.2 的全部偏差，只作量级参考

### 2.4 成功率变化

**未测得。** 要这个数，必须有**同一批任务**的改前/改后两臂 trace（`--after`），并且能给答案判分。本次输入不具备条件。

这不是遗漏而是口径：把「没测」写成「没变化」是本项目在 v3.0 反复拆掉的那类错误。

## 3. 逐工具归因（实测）

| 工具 | 调用 | 返回字符 | 结果码 | 有结构的调用 | 段数 | 段字符 |
| --- | ---: | ---: | --- | ---: | ---: | ---: |
| `list_files` | 12 | 21,315 | error:6,not_found:2,ok:4 | 4 | 722 | 19,733 |
| `search_code` | 4 | 16,214 | ok:4 | 4 | 20 | 16,198 |
| `get_signature` | 21 | 1,933 | not_found:17,ok:4 | 4 | 49 | 1,294 |
| `read_file` | 5 | 1,895 | not_found:4,ok:1 | 1 | 8 | 1,023 |
| `get_coverage_report` | 1 | 102 | not_implemented:1 | 0 | 0 | 0 |
| `list_issues` | 1 | 94 | not_implemented:1 | 0 | 0 | 0 |

「有结构的调用」= 返回被成功切成段的调用数。它小于「调用」的那些返回，本工具**不猜**其结构（多为 not_found / error 的短文本），因此不进段级统计。

## 4. 段级归因（分档，不合成单一利用率）

| 工具 | 段种类 | 段数 | 段字符 | 被引用 | 未观测到引用 | 不可判 | 被引用字符 | 未观测字符 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `list_files` | path | 722 | 19,733 | 2 | 720 | 0 | 75 | 19,658 |
| `search_code` | hit | 16 | 15,952 | 13 | 3 | 0 | 14,687 | 1,265 |
| `get_signature` | sig | 45 | 1,165 | 8 | 33 | 4 | 190 | 843 |
| `read_file` | section | 8 | 1,023 | 1 | 7 | 0 | 518 | 505 |
| `search_code` | header | 4 | 246 | 1 | 3 | 0 | 57 | 189 |
| `get_signature` | header | 4 | 129 | 1 | 3 | 0 | 32 | 97 |

读法（三档含义不同，不可合并）：

- **被引用**：值在调用之后出现在后续文本里，且此前不在上下文里。
- **未观测到引用**：本次运行没看到引用。**不等于该段无用**——可能只是这批任务没覆盖；且 `after` 缺中间推理文本，这个方向是**漏判**。
- **不可判**：值太短或太常见（撞车概率高），或可区分值本来就在上下文里。**既不计分子也不计分母**。

「被引用字符」天然偏高：大段只要命中一个值就整段计入。合法用法是配着段数与命中比例一起看，不能单独引用。

### 4.1 证据链样例：被引用但命中很稀的大段

| 任务 | 工具 | 段 | 段字符 | 命中/可判 | 命中的值（样例） |
| --- | --- | --- | ---: | ---: | --- |
| contextbudget 的 ag | `search_code` | `hit:scripts/run_context_audi` | 1,960 | 2/60 | `run_all`、`scripts/run_context_audit.py` |
| observability 的 se | `search_code` | `hit:scripts/run_context_audi` | 1,960 | 3/58 | `get_in_memory_exporter`、`scripts/run_context_audit.py`、`spans` |
| observability 里的 g | `search_code` | `hit:scripts/run_context_audi` | 1,960 | 3/59 | `exporter`、`scripts/run_context_audit.py`、`spans` |

这些段被判为「被引用」，但整段 1500+ 字符里只有个位数个值出现了。**这不构成「整个返回都被用上」的证据**——它恰好是分层加载的候选。

## 5. 跨工具信息流（本工具里最硬的证据）

未观测到跨工具信息流：没有任何工具返回的值出现在**后续工具调用的参数**里。

边界：「值进了最终答复但没再调工具」不在本表里（见 §4）。

## 6. 改动提案（可执行 + 可逆 + 带代价）

### 6.1 定义侧·裁撤未调用工具

- **对象**：21 个工具：git_log, git_blame, git_diff, create_branch, open_pull_request, review_pull_request…
- **动作**：从下发给模型的工具清单里移除，保留实现（随时可加回）
- **证据**：本批 32 个任务、73 轮里这 21 个工具被调用 0 次；每个定义每轮都要重发，故代价 = 各定义字符之和 × 轮数
- **证据强度**：中（「本批没用到」是这一批的观测，不是通用事实）
- **可逆**：是
- **影响调用数**：0
- **代价（字符口径，实测）**：598,016 字符 ⇒ 约 116,478 tokens（换算）

### 6.2 返回侧·大段改分层加载

- **对象**：search_code 的 hit 段（3 段 ≥200 字符）
- **动作**：改成「先返回段索引（名字 + 字符数），命中后再取该段全文」——**不是删除**：这些段仍可取到，只是不再常驻上下文
- **证据**：16 段里这 3 段在本次运行中**未观测到任何引用**；合计 1,265 字符，各乘其剩余轮数
- **证据强度**：弱（`after` 不含模型中间推理文本 ⇒ 会漏判引用；处置取可逆动作）
- **可逆**：是
- **影响调用数**：1
- **代价（字符口径，实测）**：1,265 字符 ⇒ 约 246 tokens（换算）

### 6.3 返回侧·小段聚合改返回 shape

- **对象**：list_files 的 path 段（722 个小段，合计 19,733 字符）
- **动作**：改返回 shape 而不是逐个裁：加**必填的收窄参数**（前缀/分页/上限），或只回索引摘要（名字 + 字符数），需要哪条再取哪条。**不是删字段**：内容仍可取到
- **证据**：722 个小段（每段 <200 字符）合计 19,733 字符，其中 19,658 字符在本次运行中**未观测到任何引用**（720 段）
- **证据强度**：弱（`after` 缺模型中间推理文本 ⇒ 会漏判引用；且收窄参数会改变模型行为）
- **可逆**：是
- **影响调用数**：4
- **代价（字符口径，实测）**：22,339 字符 ⇒ 约 4,351 tokens（换算）

**处置纪律**：所有提案都是**可逆**的（保留实现、内容仍可取回），没有一条是删除信息。依据是项目一贯的判据——证据弱时选可逆的动作，别让一个「我没观测到」的判断去执行有代价且不可逆的操作。

## 7. 改前/改后对照（验证环节）

本次未提供改后一臂 ⇒ **对照未做**。

## 8. 不确定性 / 未观测 / 不可迁移

### 8.1 本次**未观测**的量（不是 0，是没测）

- 改后重跑的成功率（除非 §7 给出了对照）
- 重试造成的浪费（本 loop 不重试，`retry_count` 恒为 0 ⇒ 未观测）
- span 之间的任务归属（被观测系统缺 run 级父 span，见 §1.1）

### 8.2 不可迁移的部分

- **token 侧结论可迁移**：prompt token 数由上下文结构决定，换成更大的模型，被裁掉的那段字符仍然不会进上下文。
- **成功率侧只在本地口径下成立**：本批用的是本机小模型，更大模型对同一处上下文裁剪的敏感度**可能不同**（可能更不敏感，也可能因为少了关键线索而更早走偏）。要迁移必须先重跑。
- **段切分规则是跟着这批工具写的**：`read_file` 按 def/class 块切，前提是返回为 Markdown 代码块。换成结构化 JSON 返回的工具，切分要换实现，结论不能照搬。

### 8.3 方法本身的已知盲区

- 段级归因只扫 **ASCII 标识符与数字**；中文散文里的引用判不了，这类一律进「不可判」档。
- **字段名 ≠ 字段值**：模型提到字段名而没用它的值，不计为引用。
- 大段只要命中一个值就整段计入「被引用」⇒「被引用字符」天然偏高。
- 同一轮内的兄弟调用互不进对方的 `before`/`after`（模型是同一条消息发的），所以同轮并行调用之间的信息流**未观测**。
- 本工具**不产出单一的「节省百分比」**：段级「未观测到引用」是弱证据，合成一个百分比会让弱证据看起来像结论。分档与分项才是可被反驳的形态。

## 9. 复现

```bash
findata-context-economist --trace examples/context-audit --tag all3b-fx --label examples/context-audit --corpus-root C:/Users/10393/WorkBuddy/Worktrees/FinData-Agent/ctxbench-frozen-noleak --grader keyword --usd-per-mtok 0.4 --out-dir examples/context-economist
```

本工具的输出是**确定性**的（除时间戳）：输入 trace 不变，报告的每个数字都不变。唯一的不确定性来自 `--usd-per-mtok`（调用方给）与 2.1 的换算系数（本批实测）。
