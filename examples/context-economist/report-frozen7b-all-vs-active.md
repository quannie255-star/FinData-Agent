# 上下文经济学报告 · frozen7b-all-vs-active

> 本报告由 findata-context-economist 从 trace 自动生成。文中「实测」指 trace 里原样记录的 provider usage 或本项目数的确定性字符数；「换算」指由实测比值推出的估算值；「未测得」指本次输入不具备测它的条件。
> 生成时间：2026-09-20 20:44:56

## 1. 本报告读的是什么

| 项 | 值 |
| --- | --- |
| runs 增强层 | `examples/context-audit/runs-all7b-fz.jsonl` |
| OTel span 层 | `examples/context-audit/spans-all7b-fz.jsonl` |
| 任务数 | 32 |
| 工具调用 | 52 次 |
| 模型调用轮数 | 83 |
| 模型 | qwen2.5:latest |
| 语义约定 | `gen-ai-stable-2026` |
| 工具清单识别 | tools_chars 完全匹配（11559 字符）⇒ 本批带的是「all」清单 |

### 1.1 输入层的能力边界（来自数据层，不是报告作者补的）

- span 之间**没有 run 级父 span**（52 个工具 span 对应 52 个不同 trace_id）⇒ 只用 span 无法把调用归到具体任务。这是**被观测系统**的打点缺口，不是读法问题；本工具需要 `gen_ai.invoke_agent`（或等价）级父 span 才能做逐任务归因
- trace 里不含**系统提示**与模型的中间推理文本 ⇒ 重建的 `before` 缺系统提示（可能把「本就在系统提示里」的值误判成新信息），`after` 只含最终答复与后续调用参数（**会漏判**引用，方向单一）

### 1.2 重建对账（字段级归因的准入闸门）

用 trace 里记录的参数重放工具，语料根 `C:\Users\10393\WorkBuddy\Worktrees\FinData-Agent\ctxbench-frozen`。共 52 次调用：**52 次重放字符数与记录值完全相等**（覆盖率 100.0%），0 次不一致、0 次参数不可解析、0 次无语料根。

**不一致的调用一律退出字段级统计**（不计分子也不计分母）——重建出来的内容与当时返回的内容不是同一份，基于它做的归因没有意义。

## 2. 三组数字

### 2.1 换算基准（本批实测，不是查表常数）

从本批 83 个模型调用实测：`prompt_tokens / (messages_chars + tools_chars)` 合计 0.1828、中位 0.1936 token/字符（p10–p90 = 0.1661–0.1959）。

分子是 provider usage（实测），分母是本项目数的字符（确定性）。**用这个系数系列算出来的 token 是估算值**；p10–p90 的宽度就是它不可信的幅度。

### 2.2 省下的 token

- **实测**（配对任务 32 个，见 §7）：224,051 → 100,843 prompt tokens，**-123,208（-55.0%）**
- **换算（上界式估算，用于排序提案）**：各提案字符口径节省合计 809,327 字符 ⇒ 约 147,905 prompt tokens（按 2.1 的 0.1828 token/字符）

**实测与估算不是一回事**：估算只覆盖 §6 里那几条提案，而实测差是两臂之间**全部**差异的结果。两者放在一起看的意义是标定「字符换算 token」这个做法的误差量级——见 §7.1。

这个估算是**上界**，两个方向的偏差同时存在，而且方向相反：
  - 偏高：段级「未观测到引用」受 `after` 缺中间推理文本影响，是**漏判** ⇒ 被判为可省的段里混有实际被用到的内容；
  - 偏低：只算了「工具定义」与「工具返回」两项，**没有**算历史消息重发与系统提示的优化空间。

**各提案不可简单相加**：返回侧的两条提案可能作用于同一段字符（重复调用的返回既进「分层加载」也进「结果复用」）⇒ 相加会重复计算。分项数字见 §6，那里每条只算自己那一段。

**还缺一侧代价（重要）**：返回侧的「分层加载」省的是 payload 字符，但模型拿不到全文后**可能要多调一次工具**（先取索引、再取正文）——这一侧的成本**没有计入**上面的节省量。真实净收益必须由改后重跑给出。

### 2.3 折算成本

- 单价：**$0.4 / 百万 prompt token**（由调用方提供，本工具不猜）
- 换算：147,905 tokens × $0.4/M = **$0.0592 / 这 32 个任务**
- 该金额继承 2.2 的全部偏差，只作量级参考

### 2.4 成功率变化

- 判分器：`keyword`（**弱判据**，只判关键词是否出现，判不出对错）
- 可判分任务：32 / 32
- 关键词命中：改前 8 → 改后 12
- 变化：**+12.5%**（只有改前命中 0 个 / 只有改后命中 4 个，McNemar 精确检验 p = 0.1250）

`p` 大于 0.05 时只能说**没有证据表明变了**，不能反过来说**证明了没变**（样本量与判分器都撑不起那个结论）。

## 3. 逐工具归因（实测）

| 工具 | 调用 | 返回字符 | 结果码 | 有结构的调用 | 段数 | 段字符 |
| --- | ---: | ---: | --- | ---: | ---: | ---: |
| `search_code` | 25 | 139,716 | error:3,not_found:11,ok:11 | 11 | 64 | 138,779 |
| `read_file` | 4 | 43,935 | ok:4 | 4 | 74 | 42,943 |
| `list_files` | 6 | 3,402 | not_found:1,ok:5 | 6 | 135 | 3,273 |
| `get_signature` | 17 | 1,964 | not_found:10,ok:7 | 7 | 51 | 1,582 |

「有结构的调用」= 返回被成功切成段的调用数。它小于「调用」的那些返回，本工具**不猜**其结构（多为 not_found / error 的短文本），因此不进段级统计。

## 4. 段级归因（分档，不合成单一利用率）

| 工具 | 段种类 | 段数 | 段字符 | 被引用 | 未观测到引用 | 不可判 | 被引用字符 | 未观测字符 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| `search_code` | hit | 53 | 138,107 | 27 | 26 | 0 | 87,353 | 50,754 |
| `read_file` | section | 74 | 42,943 | 39 | 31 | 4 | 29,219 | 13,628 |
| `list_files` | path | 135 | 3,273 | 42 | 93 | 0 | 970 | 2,303 |
| `get_signature` | sig | 44 | 1,410 | 14 | 26 | 4 | 490 | 785 |
| `search_code` | header | 11 | 672 | 0 | 11 | 0 | 0 | 672 |
| `get_signature` | header | 7 | 172 | 2 | 2 | 3 | 44 | 64 |

读法（三档含义不同，不可合并）：

- **被引用**：值在调用之后出现在后续文本里，且此前不在上下文里。
- **未观测到引用**：本次运行没看到引用。**不等于该段无用**——可能只是这批任务没覆盖；且 `after` 缺中间推理文本，这个方向是**漏判**。
- **不可判**：值太短或太常见（撞车概率高），或可区分值本来就在上下文里。**既不计分子也不计分母**。

「被引用字符」天然偏高：大段只要命中一个值就整段计入。合法用法是配着段数与命中比例一起看，不能单独引用。

### 4.1 证据链样例：被引用但命中很稀的大段

| 任务 | 工具 | 段 | 段字符 | 命中/可判 | 命中的值（样例） |
| --- | --- | --- | ---: | ---: | --- |
| triage.py 里 CAT_SC | `search_code` | `hit:src/findata/agentops/tri` | 6,871 | 1/107 | `schema` |
| contextbudget 的 sc | `search_code` | `hit:scripts/compare_audit_ar` | 5,380 | 1/96 | `sorted` |
| contextbudget 的 sc | `search_code` | `hit:src/findata/contextbudge` | 4,666 | 1/64 | `tools.py` |
| contextbudget 的 sc | `search_code` | `hit:scripts/run_context_audi` | 2,638 | 1/52 | `none:` |
| triage.py 里 CAT_SC | `search_code` | `hit:src/findata/agentops/tri` | 2,613 | 1/53 | `schema` |

这些段被判为「被引用」，但整段 1500+ 字符里只有个位数个值出现了。**这不构成「整个返回都被用上」的证据**——它恰好是分层加载的候选。

## 5. 跨工具信息流（本工具里最硬的证据）

每一行都可核对：A 返回的某个值，出现在**后续调用** B 的参数里。参数由 trace 原样记录，不存在「散文里提到算不算引用」的解释空间。

| 从 | 到 | 命中实例 | 涉及段 | 样例值 |
| --- | --- | ---: | ---: | --- |
| `get_signature` | `search_code` | 1 | 1 | `defect_kind` |
| `get_signature` | `read_file` | 1 | 1 | `pyproject.toml` |
| `list_files` | `get_signature` | 1 | 1 | `tests/test_docs_consistency.py` |

「命中实例」= 命中次数（同一个值被多次用到会多次计数），**不是去重后的不同值个数**；「涉及段」= 该方向有多少个返回段贡献过值。

## 6. 改动提案（可执行 + 可逆 + 带代价）

### 6.1 定义侧·裁撤未调用工具

- **对象**：23 个工具：git_log, git_blame, git_diff, create_branch, open_pull_request, review_pull_request…
- **动作**：从下发给模型的工具清单里移除，保留实现（随时可加回）
- **证据**：本批 32 个任务、83 轮里这 23 个工具被调用 0 次；每个定义每轮都要重发，故代价 = 各定义字符之和 × 轮数
- **证据强度**：中（「本批没用到」是这一批的观测，不是通用事实）
- **可逆**：是
- **影响调用数**：0
- **代价（字符口径，实测）**：746,087 字符 ⇒ 约 136,348 tokens（换算）

### 6.2 返回侧·大段改分层加载

- **对象**：read_file 的 section 段（19 段 ≥200 字符）
- **动作**：改成「先返回段索引（名字 + 字符数），命中后再取该段全文」——**不是删除**：这些段仍可取到，只是不再常驻上下文
- **证据**：74 段里这 19 段在本次运行中**未观测到任何引用**；合计 12,486 字符，各乘其剩余轮数
- **证据强度**：弱（`after` 不含模型中间推理文本 ⇒ 会漏判引用；处置取可逆动作）
- **可逆**：是
- **影响调用数**：3
- **代价（字符口径，实测）**：12,486 字符 ⇒ 约 2,282 tokens（换算）

### 6.3 返回侧·大段改分层加载

- **对象**：search_code 的 hit 段（26 段 ≥200 字符）
- **动作**：改成「先返回段索引（名字 + 字符数），命中后再取该段全文」——**不是删除**：这些段仍可取到，只是不再常驻上下文
- **证据**：53 段里这 26 段在本次运行中**未观测到任何引用**；合计 50,754 字符，各乘其剩余轮数
- **证据强度**：弱（`after` 不含模型中间推理文本 ⇒ 会漏判引用；处置取可逆动作）
- **可逆**：是
- **影响调用数**：8
- **代价（字符口径，实测）**：50,754 字符 ⇒ 约 9,275 tokens（换算）

**处置纪律**：所有提案都是**可逆**的（保留实现、内容仍可取回），没有一条是删除信息。依据是项目一贯的判据——证据弱时选可逆的动作，别让一个「我没观测到」的判断去执行有代价且不可逆的操作。

## 7. 改前/改后对照（验证环节）

配对任务：**32** 个（改前独有 0，改后独有 0）

| 量 | 改前 | 改后 | 差 |
| --- | ---: | ---: | ---: |
| prompt tokens（逐轮求和，实测） | 224,051 | 100,843 | -123,208 |
| 工具调用次数 | 52 | 53 | +1 |
| 工具返回字符 | 189,017 | 174,200 | -14,817 |

逐任务差值：更省的 29 个 / 更贵的 3 个 / 持平 0 个，中位差 -3,328.0 tokens，符号检验 p = 2.56e-06。

用符号检验而不是 t 检验：token 差值明显长尾，t 检验会高估显著性。

### 7.1 估算 vs 实测（定义侧那一条）

- §6 的**字符口径估算**：约 136,348 tokens（换算）
- 本节**实测**差：123,208 tokens（改前 − 改后）
- 估算/实测 = **1.11×**（偏高）

这个倍数就是「用字符换算 token」这个做法的误差量级。它值得被记住：估算的用途是**排序提案**，不是替代重跑。

## 8. 不确定性 / 未观测 / 不可迁移

### 8.1 本次**未观测**的量（不是 0，是没测）

- 改后重跑的成功率（除非 §7 给出了对照）
- 重试造成的浪费（本 loop 不重试，`retry_count` 恒为 0 ⇒ 未观测）
- span 之间的任务归属（被观测系统缺 run 级父 span，见 §1.1）

### 8.2 不可迁移的部分

- **token 侧结论可迁移**：prompt token 数由上下文结构决定，换成更大的模型，被裁掉的那段字符仍然不会进上下文。
- **成功率侧只在本地口径下成立**：本批用的是本机小模型，更大模型对同一处上下文裁剪的敏感度**可能不同**（可能更不敏感，也可能因为少了关键线索而更早走偏）。要迁移必须先重跑。
- **段切分规则是跟着这批工具写的**：`read_file` 按 def/class 块切，前提是返回为 Markdown 代码块。换成结构化 JSON 返回的工具，切分要换实现，结论不能照搬。

### 8.3 方法本身的已知盲区

- 段级归因只扫 **ASCII 标识符与数字**；中文散文里的引用判不了，这类一律进「不可判」档。
- **字段名 ≠ 字段值**：模型提到字段名而没用它的值，不计为引用。
- 大段只要命中一个值就整段计入「被引用」⇒「被引用字符」天然偏高。
- 同一轮内的兄弟调用互不进对方的 `before`/`after`（模型是同一条消息发的），所以同轮并行调用之间的信息流**未观测**。
- 本工具**不产出单一的「节省百分比」**：段级「未观测到引用」是弱证据，合成一个百分比会让弱证据看起来像结论。分档与分项才是可被反驳的形态。

## 9. 复现

```bash
findata-context-economist --trace examples/context-audit --tag all7b-fz --after examples/context-audit --after-tag active7b-fz --corpus-root C:/Users/10393/WorkBuddy/Worktrees/FinData-Agent/ctxbench-frozen --grader keyword --usd-per-mtok 0.4 --out-dir examples/context-economist
```

本工具的输出是**确定性**的（除时间戳）：输入 trace 不变，报告的每个数字都不变。唯一的不确定性来自 `--usd-per-mtok`（调用方给）与 2.1 的换算系数（本批实测）。
