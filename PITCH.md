# 投递材料（自用）

README 是给"点进仓库看的人"的，这份是给"写简历/准备面试的自己"的。
数字全部来自 `examples/` 里可复现的产出与 CI，不要凭记忆改。

> **v3.0 起主线已切到「Agent 训练数据的质检与过滤」。**
> 下面第一、二节先给**新主线**的简历描述与数字（对应目标 JD 的 C 类
> Agent/LLM 岗），v0.x–v2.2 的可信数据 / A 股内容保留在后面作为「同一套
> 归因引擎换个域」的证据——投 C 类岗讲前面，投 A 类（数据工程/治理）岗
> 讲后面。

---

## 一·新、简历项目描述 · Agent 训练数据质检（v3.0，投 C 类岗用这份）

> **Findata — Agent 训练数据的质检与过滤** ｜ 个人项目 ｜ Python
>
> 针对"Agent 轨迹脏不脏、这条能不能喂训练没人管"这一空白，做了一条
> 端到端质检管线：丢进去一批 Agent 轨迹，吐出**哪些能喂训练、哪些不能、
> 为什么**，每条判定带证据链。核心难点是归因——**必填参数没传**，可能是
> agent 漏填（能力问题），也可能是工具 schema 自相矛盾（数据源的错）；
> **形态一模一样，结论完全相反**，归错了改进方向就是反的。
>
> · 真实公开语料闭环（`Salesforce/xlam-function-calling-60k`，**全量 60000 条 /
>   100011 次工具调用**）：命中 1351 条（2.25%），其中 **1286 条根因是工具
>   description 宣称有默认值而 schema 未标 default**——第一版全被判成
>   "agent 漏填"（建议换更大模型），方向完全错。
>   **注意措辞：说"命中"不说"错误"**——我只有调用没有执行结果，
>   无标签数据上"错误率"是个测不出来的数
> · 命中必须分两类：**A 样本侧 / B 工具 schema 侧**。混成一类就会把数据源的
>   错算到 agent 头上，进而丢掉本来能用的样本。
>   B 类的产物不是样本清单，是「**21 个工具 / 21 个参数要改**」——可执行动作。
>   这 21 项还分两种改法：19 项"漏了 default"（补上）、2 项"default 贴错位置"
>   （把值**移**过去，不是两边都补）——统一成"补 default"会让数据源方改得更错
> · 根因与处置拆成两维：沙箱超时**不算失败**（环境问题，重跑可能就成），
>   当负样本丢掉是浪费数据；参数写错的才丢弃。混成一维两边都没法单独调。
>   **兜底类（"我没认出来"）交人工而不丢弃**——认不出不等于样本坏
> · 无标签也能质检：槽位出处探针不依赖任何 golden 标注（生产轨迹没有标签
>   是常态），查不出处标「存疑」而不是指控幻觉
> · 零自造样本：曾自造失败样本测出归因准确率 100%，换从未参与调参的
>   held-out 集后掉到 **63.6%** —— 自造样本证明不了自己的规则，遂整体作废
>   改走真实数据，这个红灯至今留着没划掉

## 二·新、能撑住追问的数字（v3.0）

| 数字 | 出处 | 怎么来的 |
| --- | --- | --- |
| **全量** 60000 条 / 100011 次调用 | `examples/trust-filter-report.txt` | `scripts/run_trust_filter.py`（默认全量，16 秒） |
| 命中 1351 条（2.25%） | 同上 | 判定全是确定性查表，不依赖模型。**命中≠错误** |
| **1286 条 schema_contradiction** | 同上 | 不是 agent 的错，是数据源自相矛盾（21 个工具 / 21 个参数） |
| **default 贴错位置 2 项**（258 + 83 次） | 同上第四节 | `permitivity` 描述称 8.854e-12，该值却挂在 `charge`/`distance`（int 型）上 |
| **误杀 349 → 0 条样本被丢** | `git show e85dd01` | 处置新增第五档 `🔧 待修 schema`。349 是那次 2 万条抽样的口径 |
| **扔掉的探针：2162 → 0** | 本报告第八节 | 类型/默认值冲突探针全量 2162 命中，查出是表示层约定（89.1% 的 default 是字符串），**主动删除** |
| held-out 63.6% vs 合成集 100% | `docs/ROADMAP.md` R4.1 | 循环论证的量化代价，**刻意保留红灯** |
| 误报 53 条 → 0（retry_storm） | `tests/test_agentops.py` | 判据由「同名」改为「同名且同参数」 |
| 误报 45 条 → 0（unknown argument） | `tests/test_xlam_adapter.py` | 同名工具重复注册，任一份 schema 能解释就放行 |
| 364 例测试 / ruff 全绿 | CI | 数字以 CI 为准，全仓库口径必须一致 |

---

## 一、简历项目描述（可直接粘贴）

> **Findata — 可信数据增强模块（数据质量归因 + 带徽章可信报告）** ｜ 个人项目 ｜ Python
>
> 针对"Agent 与问数平台都默认喂进去的数据是好的"这一空白，做了一个挂接到
> 成熟 Agent 项目上的**可信增强模块**：任何宿主 Agent 引用数字前经
> MCP/HTTP 查询四档可信徽章（✓ 已核验 / ✓ 基线通过 / ⚠️ 仅借鉴 / ✗ 不可用），
> 徽章可点开看归因证据链（停牌/除权/截面共动等业务事实）。核心难点在于
> **停牌缺行与采集失败缺行在数据形态上完全一致**，仅靠检测无法区分。
>
> · 真实 A 股数据考试：7/7 告警全是误报（抑制 0/9）→ 知识层种子 + 截面共动
>   归因修复 → **抑制 8/9、健康分 72 → 95.8**，9 个真实案例固化为回放 golden 进 CI
> · **通用数据可信包**：任意 CSV/Excel/DuckDB/SQLite 表出带徽章报告；
>   同一数据集对比实测——通用包 60 分/11 信号无法归因 vs 领域包 100 分/停牌核验
> · 可信日报 Agent：多智能体（分析→可视化→可信审校）逐数字带徽章，LLM 解读经
>   纯规则审校拦截无徽章引用，实测审校 0 拦截；外部 MCP 宿主零依赖接入已验证
> · 自建评测门禁：合成语料告警精确率 **100%**（朴素基线 61.5%）+ 真实回放
>   golden + 364 例测试全进 GitHub Actions，归因变差即挂红
> · 服务化：同一 service 层暴露 FastAPI / MCP stdio / Python 三接口，
>   OpenTelemetry 埋点；镜像发布 ghcr.io（amd64/arm64），push 前容器冒烟

**技术栈**：Python · LangGraph · FastAPI · DuckDB · MCP · OpenTelemetry · Docker/GH Actions · akshare

**如果 JD 强调 LangGraph / RAG**：不要硬凑。见下面「高频追问」第 2、3 条——
这个项目里两者都有可讲的东西：分析层真实用了 LangGraph，归因层讲清了
为什么不该用 RAG。

---

## 二、能撑住追问的数字

| 数字 | 出处 | 为什么有说服力 |
| --- | --- | --- |
| **100% vs 61.5%** | `examples/eval-rule-triage.txt` | 同一批 13 条信号，不做归因时精确率只有 61.5%。这 38.5 个百分点是归因层存在的全部理由 |
| **84 → 92 分，告警 4 → 2** | `examples/inspection-warehouse-2026-09-07.md` | 真实数据上实测。不是跑通了，是**跑出了误报然后修掉** |
| **真实告警抑制 0/9 → 8/9，健康分 72 → 95.8** | `examples/replay-badge-report.txt`（回放 golden：`eval/golden/replay.yaml` 9/9） | v2.0 R1：2026-09-15 真实考试 7/7 误报（抑制 0/9）后，知识层种子 + 截面共动归因一次修到位。**系统知道自己什么时候在裸奔、并且知道怎么穿上** |
| **日报每个数字带徽章，无徽章数字强制拦截** | `examples/daily-badge-report-2026-09-15.md` + `tests/test_report_agent.py` | v2.0 R1 报告 Agent：LLM 解读逐数字带徽章（实测 710/472 字符、审校 0 拦截），审校纯规则、可全分支单测——「无徽章不得引用」不是口号是闸门 |
| **364 个测试 + CI 门禁（含 eval-gate + 真实回放）** | GitHub Actions | 评测指标进 CI，归因变差就红（当前 364 例 + 语义 golden + 真实回放 9 case）。这是"承诺"和"承诺被执行"的区别 |
| **多智能体 token +21%，正确率持平** | `examples/eval-agent-single-vs-multi.txt` | v0.8.0 单 vs 多智能体同题同库对比。多智能体不是免费的——但deep 路径换来 DQ 运行时背书，简单题走快路径不多花一枚 token。**成本如实入账**本身就是可信叙事的一部分 |
| **进化后抑制 62.5% → 100%，分开了规则分不开的形态** | `examples/eval-prompt-evolution.txt` | v0.9.0 难例靶子实测：合法放量（探针比值 5.17–7.43x）与单位变更故障（5.31x）比值区间重叠，任何阈值规则必然牺牲一边；GEPA 式反思进化出的 prompt 凭量额一致性 + 市场共振证据做到根因/抑制/精确全 100%。**进化产物过了与代码相同的 eval-gate 才合入**（v1→v1.1→v2 版本档案可回滚），全程约 170 次 LLM 调用（几分钱）如实入账 |
| **同一数据集：通用包 60 分 vs 领域包 100 分** | `examples/domain-vs-generic-comparison.md` | v2.2.0 对比页实测：同一份 A 股数据，通用基线包 11 个信号无法归因、8 列仅借鉴；领域包把 2 段停牌缺口核验成 ✓ 已核验。「没查出毛病」和「证明可靠」的差值，就是领域包架构的存在理由——这是产品含金量的直接展示 |
| **低置信记忆不得改变抑制决策** | `tests/test_memory.py` | v0.9.0 经验记忆的污染防护门禁用例：毒化记忆下抑制决策与 prompt 逐字节不变。记忆会污染是自进化系统真正的风险，「宁缺毋滥」有测试背书 |

---

## 三、30 秒电梯演讲

> 我做的是挂到 Agent 项目上的「可信增强模块」。起因是做问数的开源方案很多，
> 但它们都默认"喂进去的数据是好的"——这个前提在真实生产里不成立。
>
> 真正难的不是检测到异常，是**停牌导致的缺行和采集失败导致的缺行，
> 在数据形态上完全一样**。只会检测的工具必然淹没人。
>
> 所以核心是归因层：结合交易日历、公司事件和截面共动性，判断这个异常
> 该不该报，并把它变成四档可信徽章——宿主 Agent 引用任何数字前查一次
> 就知道能不能引用、凭什么。这套东西在真实 A 股数据上考过试：
> 第一次考试 7 条告警全是误报，我把修复固化成回放 golden 进 CI，
> 现在真实回放抑制 8/9。同一段数据，通用基线包给 60 分、领域包给
> 100 分——这个差值就是这个产品的全部论点。

---

## 四·新、高频追问应答（v3.0，投 C 类岗先看这份）

> **完整版见 [`docs/interview-qa.md`](docs/interview-qa.md)**：37 条拷问逐条作答 +
> 五个红旗的正面回应 + 「不自证的部分」清单。下面这份是压缩版，
> 面试前背这份就够，被追问细节时再翻完整版。

### Q1'：Langfuse / LangSmith 不也在做这件事吗？

目标不一样，不是同一件事的竞争。它们做的是「记录 → 打分 → 改进 agent」，
服务的是**想把 agent 调好的人**；我要的是「从海量轨迹里筛出能喂训练的
正负样本」，服务的是**要生产训练数据的人**。第三方对比文（Inference.net,
2026-06）原话：两者都擅长存储与检查单条 trace，但**都不告诉你几千次运行
里究竟是什么一直在出错**——而"几千次里什么一直错"正是筛选训练数据要的。

### Q2'：没有 ground truth，你怎么证明过滤是对的？

分两层，**不许混着说**：
- 判定层全是**确定性查表**（工具名在不在注册表、必填项有没有传），
  不依赖模型，所以这一层没有准确率问题——但代价是**查不到的脏也照查不出**
  （比如参数值语义错了），这个必须认。
- 归因层**确实测不了准确率**：真实数据没有根因标签。我早先自造样本测出
  100%，换成从没参与调参的 held-out 集只有 **63.6%**——这个红灯我到现在
  没划掉。面试时能讲清"为什么测不了"，比报一个 100% 可信。

### Q3'：JD 写了「数据合成」，你为什么不做？

**刻意不做，理由能讲。** 自造的失败样本形状必然贴合自己写的规则——
合成集 100%、held-out 63.6%，这 36 个点就是循环论证的量化代价。而且
面试官一追问"你怎么保证合成的样本像真的"，答不上。真实语料不需要回答
这个问题。代价是这个关键词只能靠"筛 + 导出标准格式"沾边，不能说我会造
数据。**取舍讲清楚，比硬凑一个合成器强。**

### Q4'：1286 条 schema_contradiction 怎么发现的？怎么确定不是 agent 的错？

**第一版我判错了，全判成 agent 漏填**，给出的改进建议是"换更大的模型"。
后来抽查原始 schema 才看到：`permitivity` 的 description 写着
"default is 8.854e-12"、schema 里却没标 `default`——**错的是数据源**。
形态（必填参数没传）和"agent 能力问题"一模一样，结论完全相反。

这条是本项目在真实数据上最锋利的一次验证：**检测不难，难的是判断该怪谁。**
现在它单列一个根因，且必须排在硬错误分类之前——排错顺序就会被笼统归成
"参数写错"，改进方向整个反过来。

**第二层更硬的证据（`git log -p src/findata/agentops/adapters/xlam.py`）**：
那 21 个待修项里，有 2 项不是"忘了写默认值"，而是**默认值写到了别的参数上**。
原始 JSON（`row 65`）长这样：

```json
{"charge":     {"description": "Charge in coulombs.",   "type": "int", "default": 8.854e-12},
 "distance":   {"description": "Distance from charge.", "type": "int", "default": 8.854e-12},
 "permitivity":{"description": "Permitivity of the space, default is 8.854e-12.",
                "type": "float"}}
```

8.854e-12 是**真空介电常数**，不可能是以库仑计的电荷量或以米计的距离的
默认值——值不是"没写"，是**落错了位置**。用"作者疏忽"解释不了这个形状。
所以这两类的处置动作必须分开报：19 项"补上 default"，2 项"把值**移**过去"。
统一成一句"补 default"，数据源方会给 `charge` 和 `permitivity` **同时**标上
8.854e-12——改完错得更彻底。

（同时按证据强度如实分档：`mean_confidence_interval` 那一项弱得多，
0.95 当标准差并非不可能。报告把两种强度都摆出来，不替读者拍板。）

### Q5'：沙箱做到什么程度？是真容器吗？

**不是容器，是进程级隔离，这点我不吹。** 真起子进程、真跑到阈值被杀、
真拿到 `TimeoutExpired` 原文——但防不住恶意代码，要那个得用容器，而本机
拉不到 `registry-1.docker.io`，做不到就不假装做了。内存限制在 Windows 上
也没有（`resource` 模块不存在），代码明确标 `memory_limit_applied=False`，
不静默降级。另外 gRPC 那套 `deadline exceeded` 现在**仍识别不了**。

### Q6'：还有什么没做完？

- **归因准确率测不了**（真实数据无标签）——不是没做，是**在这个口径下
  无法测**，红灯留着当素材
- 容器级沙箱：本机做不了
- **B 类岗（数据科学/分析）明确放弃**：无 A/B 实验、无统计建模，这两份
  JD 靠别的项目凑，findata 不负责覆盖
- 陌生宿主集成证据：我自证不算数，得有个没看过这个项目的人跑一遍

---

## 附、高频追问应答（v0.x–v2.2，投 A 类数据工程/治理岗用这份）

### Q1：真实数据上跑过吗？

跑过，而且**第一次考试是不及格的**——这才是最有价值的部分。2026-09-15
真实巡检报了 7 条告警，我逐条归因后发现 7/7 全是合法事件（4 段停牌缺口 +
3 次市场行情放量），抑制 0/9、健康分 72。根因不是算法错，是**知识层从未
上线**：`corporate_event` 表 0 行，抑制规则在生产上是死代码。我把整个复盘
写成了文档（`docs/reviews/2026-09-15-attribution.md`），然后按复盘落地修复：
4 笔公告级核实的停牌种子入库 + 漂移归因先查截面共动（全市场/指数/板块三判据）。

修复后用真实仓库快照做**回放 golden**（9 个案例逐条断言根因/抑制/证据链，
进 CI）：抑制 8/9、健康分 95.8。唯一残留如实：中芯国际 2023-03 的 AI 行情
是**个股级**孤立事件，截面判据不命中，需要公告/新闻这类外部事实源。
系统知道自己什么时候在裸奔（知识层状态行进日报）、并且知道怎么穿上
（P0a-P0c 全落地）——这套经历比一个好看的数字值钱。

### Q2：LangGraph 是怎么用的？

按需使用，不滥用。问数多智能体（Supervisor + Schema/Query/Verifier/Triage
四子 Agent）和日报报告 Agent（分析→可视化→可信审校）真实用了 LangGraph——
它们有运行时才能决定的回环：工具调用多轮、验证员追问、审校打回重写。

而巡检主链路（探针 → 归因 → 路由 → 渲染）刻意**不用**：它是确定性 DAG，
没有"走一步看一步"的分支，套一层图是把确定的东西变成不确定的，还多一个
状态机要测。连复杂度路由都用确定性规则而不是 LLM——路由本身也要花钱。
另外 RL 环境封装（M14，封存待 GPU）也刻意没用 LangGraph 自研循环，
对标过 SkyRL/slime 后认为窄 agent 的循环自研更可控。

### Q3：为什么没用 RAG / 向量库？

归因要的不是"相似的文档"，是**精确的业务事实**：这只股票这段区间是不是停牌、
哪天除权。这些是查表问题，`corporate_event` + SQL 直接命中。

更要紧的是，相似度检索会返回"看起来像"的事实，而在数据质量场景里，
**一个像的事实比没有事实更危险**——它会让系统理直气壮地抑制掉一条真告警。
宁可说"不知道"（`UNKNOWN` 降级给规则归因器），也不能编一个像的。

### Q4：100% 精确率是不是因为数据太简单？

语料确实是我构造的，这点不回避。但设计上有三点让它不是自欺欺人：

1. **有朴素基线对照**。同样 13 条信号，不做归因是 61.5%——说明这 13 条里
   确实有 5 条是"看起来像故障"的合法事件，不是送分题
2. **合成语料证明机制对，真实回放证明事实够，两者缺一不可**。合成语料没
   暴露的问题，是 2026-09-15 真实考试暴露的（7/7 误报）；修复后我把 9 个
   真实案例固化成回放 golden 进 CI（`eval/golden/replay.yaml`），真实数据上
   的结论从此也有门禁管着，不只是合成语料漂亮
3. **难例靶子专门对着规则的软肋造**。合法放量脉冲与单位变更故障的探针比值
   区间数学上重叠，任何阈值规则无缝隙可插（回归断言钉死）——这条难例后来
   由进化后的 LLM prompt 分开（v0.9，封存），规则引擎的局限被如实测量

### Q5：这个项目最难的取舍是什么？

**撤回过一个改动，后来又用另一条路把它做成了。** 真实数据上报了一条
"成交量放大 5.4 倍"，核查后发现是 2023 年 7 月券商政策行情，合法。我试着
加归因规则区分它和"单位变更"，失败了：探针算的 10 日均量/前 30 日均量把
评测注入的 20x 稀释成 5.14x，和真实放量的 5.40x 数值上几乎重合。硬塞规则
会让根因准确率从 100% 掉到 85.7%、评测门禁挂掉。**为了修一条告警去牺牲
门禁是负收益**，所以我撤回代码，把失败的判据和数值写进已知局限。

后来（v2.0）的正确解法不是更好的阈值，是**换证据源**：横向问一遍同期市场——
全市场放量分布、指数量能、板块同伴（复盘发现这些事实就在仓库里，一行外部
数据都不用加）。截面共动规则上线后，同一段真实行情被正确抑制，合成语料
门禁零回归。两条经历合起来是最好的答案：**知道自己什么时候在裸奔，并且
知道裸奔的解法通常是缺事实，不是缺算法。**

### Q6：还有什么没做完？

- ~~真实数据上抑制率还是 0~~（v2.0 已解决：2026-09-15 真实考试 7/7 误报后，
  知识层种子 + 截面共动归因落地，真实回放抑制 8/9，见
  `examples/replay-badge-report.txt`）唯一残留：中芯国际 2023-03 孤立行情
  是**个股级**事件，截面判据不命中，需要公告/新闻这类外部事实源（P2 观察级）
- 漂移归因的"单位变更 vs 个股孤立行情"仍需外部事实（交易所口径变更公告）；
  全市场/板块级行情已可由仓库自证判别
- 如果要做"查询时可信"（口径语义对齐 + 引用溯源 + 置信标注），那是另一条主线
  ——v2.0 已在这条线上：徽章层 + 报告 Agent 已落地，引擎 API（R3）在计划中

---

## 五、投递前自检（v3.0）

- [ ] **README 第一屏是 v3.0 主线**（Agent 训练数据质检），不是旧的"可信增强模块"
- [ ] GitHub Description / Topics 已按 `docs/handoff.md` 的新文案改
      （旧的写的是"可信数据增强模块"，**必须换**）
- [ ] `uv run pytest` 364 例全绿 + CI badge 正常显示
- [ ] `examples/` 三份主证据能打开：`trust-filter-report.txt`（真实语料闭环）、
      `sandbox-signals.txt`（沙箱真实信号）、`agent-trace-report.txt`（本机轨迹）
- [ ] **能一句话讲清 349 条 schema_contradiction 为什么不是 agent 的错**——
      这是面试里唯一能甩开别人的点，讲不清就等于白做
- [ ] 能说出 held-out 63.6% 这个红灯**为什么没划掉**（不许临时编个好看的数）
- [ ] **能讲清「误杀 349 条」那次改判**（`docs/interview-qa.md` 红旗 #2）——
      证据是「58105 个工具里 `required` 字段 0 个」，即"缺参数=漏填必填项"
      是**我自己设的口径**。被判错不丢分，**讲不出怎么发现的才丢分**
- [ ] 被问「timeout 一律归环境是不是掩盖 agent 缺陷」时，能说清
      **为什么现在刻意不改**（本批 0 命中＝未验证，此时改判据就是
      「凭想象设口径」——同一个错刚犯过一次），以及多证据判据的三步修法
- [ ] 被问「无标签凭什么说命中是错」时，先纠正**措辞**：报告写的是
      **命中率**，且显式注了「命中数不等于错误数」「未命中≠确认正确」

### v0.x–v2.2 遗留（投 A 类岗时才需要）

- [ ] tag `v2.2.0` 触发 release workflow，`ghcr.io/quannie255-star/findata-agent:2.2.0`
      能 pull（多架构）——发布流程见 `docs/distribution.md`
- [ ] `examples/` 旧归档能打开：带徽章日报、对比页、MCP 宿主 transcript、
      通用包报告 ×2、回放报告
- [ ] 远端那个游离的 `main` 分支（2026-05-16 的 sentiment.py 草稿，
      与本项目无共同祖先）—— 建议删除，但**不可逆，自己确认后再删**：
      `git push origin --delete main`
