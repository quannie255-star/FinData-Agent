# 投递材料（自用）

README 是给"点进仓库看的人"的，这份是给"写简历/准备面试的自己"的。
数字全部来自 `examples/` 里可复现的产出与 CI，不要凭记忆改。

---

## 一、简历项目描述（可直接粘贴）

> **Findata — 面向金融数据的数据质量监控 Agent** ｜ 个人项目 ｜ Python
>
> 针对数据仓库异常告警噪音过高的问题，设计了「检测—归因—路由」三层流水线：
> 7 类探针覆盖缺失/停更/漂移/单位跳变等故障，归因层结合交易日历与公司事件
> （停牌、除权除息、新股上市）区分「合法业务状态」与「真实故障」，并按严重度
> 路由告警。核心难点在于**停牌缺行与采集失败缺行在数据形态上完全一致**，
> 仅靠检测无法区分。
>
> · 自建评测门禁：确定性故障注入 + 13 条信号对照，告警精确率 **100%**，
>   而不做归因的朴素基线仅 **61.5%**
> · 在真实 A 股数据（25 只标的 / 3396 行）上验证，接入交易所真实交易日历后
>   消除春节误报，**告警从 4 条降到 2 条、健康分 84 → 92**
> · 服务化：同一份 service 函数同时暴露为 FastAPI（HTTP）与 MCP stdio 工具，
>   避免 UI 与 Agent 两套逻辑漂移；OpenTelemetry 埋点 + GitHub Actions 门禁
> · 镜像发布至 ghcr.io（amd64/arm64），push 前容器冒烟，坏镜像不进 registry

**技术栈**：Python · FastAPI · DuckDB · MCP · OpenTelemetry · Docker/GH Actions · akshare

**如果 JD 强调 LangGraph / RAG**：不要硬凑。见下面「高频追问」第 2、3 条——
能讲清"为什么不用"比"用过"更值钱，而且这个项目里确实有可讲的东西。

---

## 二、三个能撑住追问的数字

| 数字 | 出处 | 为什么有说服力 |
| --- | --- | --- |
| **100% vs 61.5%** | `examples/eval-rule-triage.txt` | 同一批 13 条信号，不做归因时精确率只有 61.5%。这 38.5 个百分点是归因层存在的全部理由 |
| **84 → 92 分，告警 4 → 2** | `examples/inspection-warehouse-2026-09-07.md` | 真实数据上实测。不是跑通了，是**跑出了误报然后修掉** |
| **109 个测试 + CI 门禁** | GitHub Actions | 评测指标进 CI，归因变差就红。这是"承诺"和"承诺被执行"的区别 |

---

## 三、30 秒电梯演讲

> 我做的是一个数据质量监控 Agent。起因是我发现做问数的开源方案很多，
> 但它们都默认"喂进去的数据是好的"——这个前提在真实生产里不成立。
>
> 真正难的不是检测到异常，是**停牌导致的缺行和采集失败导致的缺行，
> 在数据形态上完全一样**。只会检测的工具必然淹没人。
>
> 所以我做的是归因层：结合交易日历和公司事件，判断这个异常该不该报。
> 我建了评测门禁——同一批信号，不做归因精确率 61.5%，做了是 100%。
> 也拿真实 A 股数据跑过，第一版报的 4 条告警里 3 条是春节误报，
> 接了真实交易日历后修掉了。

---

## 四、高频追问应答

### Q1：真实数据上跑过吗？

跑过，而且**第一版是错的**——25 只标的 3396 行，4 条告警里 3 条是春节误报，
因为硬编码节假日表只覆盖 2024-2025。春节是全市场同时休市，那些日子在数据里
没有任何观测，所以"数据里出现过就是交易日"这种推断永远推不出来。
接了交易所真实交易日历（1990 至今 8797 天）后修掉，健康分 84 → 92。

剩下 2 条我逐条核查过是真信号：600030 缺的 6 个交易日，同窗口其他标的有数据——
个别缺失而非集体休市，判定"回补漏采"成立。

### Q2：为什么没用 LangGraph？

核心流水线是确定性 DAG：探针 → 归因 → 路由 → 渲染。没有运行时才能决定的分支、
没有回环。LangGraph 解决的是"下一步取决于上一步结果"的编排——多轮工具调用、
条件跳转、人工介入。这里一条直线跑到底，套一层图是把确定的东西变成不确定的，
还多一个状态机要测。

什么时候会真的需要：如果做"查询时可信"——证据不足时反问、多轮补证据——
那是有回环的对话状态机，那时才该上。

### Q3：为什么没用 RAG / 向量库？

归因要的不是"相似的文档"，是**精确的业务事实**：这只股票这段区间是不是停牌、
哪天除权。这些是查表问题，`corporate_event` + SQL 直接命中。

更要紧的是，相似度检索会返回"看起来像"的事实，而在数据质量场景里，
**一个像的事实比没有事实更危险**——它会让系统理直气壮地抑制掉一条真告警。
宁可说"不知道"（`UNKNOWN` 降级给规则归因器），也不能编一个像的。

### Q4：100% 精确率是不是因为数据太简单？

语料确实是我构造的，这点不回避。但设计上有两点让它不是自欺欺人：

1. **有朴素基线对照**。同样 13 条信号，不做归因是 61.5%——说明这 13 条里
   确实有 5 条是"看起来像故障"的合法事件，不是送分题
2. **真实数据上验证过，并且暴露了问题**。合成语料没暴露春节误报和
   `list_date` 断链，是真实数据跑出来的。我在 README 里把这两个坑都写明了

另外真实数据上**抑制率是 0**——553 条除权除息事件一条都没落在告警窗口内。
这句话我也写在 README 里了。合成语料的漂亮数字是设计出来的对照，
真实事件源才是下一步。

### Q5：这个项目最难的取舍是什么？

**撤回过一个改动。** 真实数据上报了一条"成交量放大 5.4 倍"，我核查后发现
同期价格上涨 14.8%，是 2023 年 7 月券商政策行情，合法。我试着加归因规则
区分它和"单位变更"，失败了：探针算的 10 日均量/前 30 日均量把评测注入的
20x 稀释成 5.14x，和真实放量的 5.40x 数值上几乎重合。硬塞规则会让根因
准确率从 100% 掉到 85.7%、评测门禁挂掉。

**为了修一条告警去牺牲门禁是负收益**，所以我撤回代码，把失败的判据和数值
写进 README 的"已知局限"。

### Q6：还有什么没做完？

- 真实数据上抑制率还是 0：停牌接口只给"此刻谁停着"的快照，没有历史起止日。
  缺的不是算法，是事实来源
- 成交量漂移仍分不清"单位变更"和"市场放量"，需要交易所口径变更公告这类外部事实
- 如果要做"查询时可信"（口径语义对齐 + 引用溯源 + 置信标注），那是另一条主线，
  架构上要加 LangGraph 编排层

---

## 五、投递前自检

- [ ] `ghcr.io/quannie255-star/findata-agent:0.6.0` 能 pull（已验证多架构）
- [ ] GitHub 仓库 description / topics 已补
- [ ] CI 全绿 badge 正常显示
- [ ] `examples/` 里的 4 份产出能打开
- [ ] 远端那个游离的 `main` 分支（2026-05-16 的 sentiment.py 草稿，
      与本项目无共同祖先）—— 建议删除，但**不可逆，自己确认后再删**：
      `git push origin --delete main`
