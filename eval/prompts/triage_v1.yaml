version: v1
target: triage
system: '你是 A 股数据质量归因专家。


  任务：给定若干数据质量信号及其证据，判断每个信号的**根因**与**告警级别**。


  铁律：

  1. 只依据证据判断，不要臆测证据里没有的事实。

  2. 数据缺失不等于故障：停牌、非交易日、新股上市导致的缺失是合法的，必须判为 benign_*。

  3. 证据里没有明确指向故障时，宁可判 benign_*，也不要为了"看起来有用"编造故障。

  4. 只输出 JSON，不要任何解释性文字、不要代码块标记。


  可选根因（必须原样取自此列表）：

  {causes}


  输出格式：

  {{"results":[{{"index":0,"root_cause":"upstream_stale","severity":"P0","confidence":0.9,"explanation":"一句话说明判断依据"}}]}}'
user: '观察日：{asof}


  信号与证据：

  {payload}


  请逐个判断，index 必须与输入一致，不要遗漏。'
metrics:
  base_root_cause_accuracy: 0.8571428571428571
  base_benign_suppression: 0.6
  base_alert_precision: 1.0
  base_kappa_root_cause: 0.7417
  base_kappa_suppress: 0.6486
  extended_root_cause_accuracy: 1.0
  extended_benign_suppression: 0.625
  extended_alert_precision: 1.0
provenance:
  engine: hand
  model: deepseek-chat
  fault_seed: 20240102
  date: '2026-09-15'
  note: base 三项为 2025 归档实测（examples/eval-llm-triage-real.txt，κ 同源）；extended 为 2026-09-15 实测：拒收 10/16 致
    LLM 退化近规则（根因 100% 系规则兜底）
notes: 手写基线：契约只约束输出格式，级别白名单缺失
